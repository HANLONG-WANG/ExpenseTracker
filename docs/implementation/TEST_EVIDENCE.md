# Test Evidence

Last updated: 2026-08-06 (Asia/Tokyo)
Evidence policy: a `VERIFIED` ledger row must cite an evidence ID below containing the exact command/test, environment and passing result. Implementation existence alone is not verification. Device-only claims cannot cite Robolectric or JVM-only evidence.

Remote CI policy: workflow parsing and local task execution prove only that CI is configured and callable. A remote GitHub Actions result may be marked verified only with a run URL/run ID and immutable artifact reference. No such remote evidence is present in this repository as of this update.

## Environment

- P01 starting commit: `cb4d66e581c1c5e55c02c64089a5461ac9bae249`
- Host: Fedora Linux, kernel `7.1.4-204.fc44.x86_64`
- Python: 3.14.6
- PyYAML: 6.0.3
- jq: 1.8.1
- Java/Javac: Eclipse Temurin 17.0.20+8
- Android SDK: Platform 36 revision 2; Build Tools 36.0.0
- adb/platform-tools: 37.0.1
- Build: Gradle Wrapper 9.5.1, AGP 9.3.1, Kotlin 2.4.10, KSP 2.3.10
- Emulator: 37.1.11; API 28 Google APIs x86/x86_64 and API 36 Google APIs x86_64 images installed
- Device acceleration: PASS — `/dev/kvm` is `crw-rw-rw- root:kvm 10,232`; emulator acceleration check reports `KVM (version 12) is installed and usable`
- Managed-device renderer: `android.testoptions.manageddevices.emulator.gpu=host`; local NVIDIA 610.43.03 OpenGL and KVM execution verified

## P00 evidence

| Evidence ID | Scope | Command / procedure | Expected result | Recorded result |
|---|---|---|---|---|
| P00-E001 | Frozen source parsing and structural completeness | `python3 scripts/validate_spec_baseline.py` | JSON, YAML and CSV parse; 434 token scalar paths; 90 sequential unique REQs; 215 unique screen IDs and routes | PASS, exit 0: 434 / 90 / 215 / 215 |
| P00-E002 | Manifest integrity and fail-closed visual exclusion | Same validator; every text read/hash enters through `assert_text_input`, and only the five textual UI inputs are hashed against `MANIFEST.sha256` | 5 text entries match; any forbidden visual filename is rejected before filesystem I/O | PASS: 5 textual entries matched; fail-closed visual access guard enabled; no fixed self-reported open count is used |
| P00-E003 | Persistent ledger exactness | Same validator plus `scripts/tests/test_p00_spec_baseline.py` | Exact ordered unique ADR/UI-ADR/INV/family sets; exact family→member mappings; frozen 94-table equality; duplicate/extra rows fail | PASS: exact 90 REQ, 215 screens, 35 INV, 20+1 ADR, 12 UI ADR, 94 logical tables, 12/7 families and 21 operation records; negative cases PASS |
| P00-E004 | Frozen text unchanged | Explicit `git diff --exit-code --` list containing only the ten allowed frozen textual inputs | Exit 0, no allowed frozen text changed; visual drafts are not included in the command | PASS, exit 0 |
| P00-E005 | Current repository baseline inventory | `git status --short`; source/config inventory excluding build caches and all four forbidden visual paths; `git diff --check`; `.pyc` scan | Current remediation changes are fully enumerated; no P04+ implementation, patch error or Python cache remains | PASS for current baseline; historical P00 command order remains explicitly non-reconstructed |
| P00-E006 | Validator syntax | `PYTHONPYCACHEPREFIX=/tmp/expense_tracker_p00_pycache python3 -m py_compile scripts/validate_spec_baseline.py` | Exit 0 | PASS, exit 0 |

Additional hygiene command: `git diff --check` and repository Python-cache scan passed on 2026-08-01 after remediation.

## P01 evidence

| Evidence ID | Scope | Command / procedure | Expected result | Recorded result |
|---|---|---|---|---|
| P01-E001 | Frozen host and Android toolchain | `java -version`; `javac -version`; `/home/hubery-fedora/Tools/Android/Sdk/cmdline-tools/latest/bin/sdkmanager --list_installed`; `/home/hubery-fedora/Tools/Android/Sdk/platform-tools/adb version` | JDK/Javac 17; Android Platform 36; stable Build Tools 36.x; working adb | PASS: Temurin 17.0.20+8, Platform 36 rev 2, Build Tools 36.0.0, adb 37.0.1 |
| P01-E002 | Machine-readable P01 baseline | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p01_baseline.py` | Exact module/version/lockfile/verification-coordinate checks; recursive nested production-source scan; no placeholder, forbidden stack, XML UI or runtime prerelease | PASS: 35 leaf modules, 5 grouping projects, 8 pure Kotlin modules, 12 feature modules, exact 37 lockfiles, 1,133 verification components, 2,339 SHA-256 entries, 0 forbidden findings |
| P01-E003 | Gradle module graph, external framework boundary and frozen versions | `./gradlew verifyArchitecture verifyFrozenVersions`; `python3 scripts/prove_p01_architecture_rejection.py` | Exact project graph; pure domain external Android/Room/Hilt/OkHttp/etc. dependencies rejected | PASS: current 40 root projects/edges accepted; injected domain OkHttp dependency rejected by `[ARCH-DOMAIN-FRAMEWORK]` |
| P01-E004 | Complete debug/release compilation and packaging | `./gradlew assemble --configuration-cache --console=plain` | All Android and pure-Kotlin modules configure and compile; app and benchmark debug/release variants package | PASS in 1m54s: 1,705 tasks, 1,308 executed, 392 from cache, 5 up-to-date |
| P01-E005 | Dependency locks and strict verification | Root and build-logic `LockMode.STRICT`; exact lock-coordinate validator; `python3 scripts/prove_p01_strict_lock_rejection.py`; normal strict aggregate build | Missing lock state must fail while the complete locked build passes | PASS: exact 37 lockfiles; all lock coordinates have verification metadata; injected resolvable unlocked configuration fails with Gradle strict-lock diagnostic |
| P01-E006 | Aggregate P01 build, lint and test | `./gradlew p03Check --configuration-cache --no-parallel --dependency-verification=strict --rerun-tasks --no-build-cache --console=plain` | Inherited P01 assemble/architecture/version/KSP tasks plus all later P02/P03 checks pass fresh | PASS in 44s: 2,110/2,110 actionable tasks executed; configuration cache stored |
| P01-E007 | Frozen-source and worktree hygiene regression | Four Python validators; explicit allowed frozen-text diff; `git diff --check`; recursive production placeholder and Python-cache scans | P00 baseline intact; no frozen text/placeholder/patch/cache defect | PASS, exit 0 for all checks; no visual-open count is inferred |

P01 intentionally contains no business implementation and therefore no device/UI/migration/screenshot/accessibility/performance case. Gradle `test` tasks completed successfully but were `NO-SOURCE`; they are recorded only as proof that the module test variants configure, not as behavioral verification. Device-only evidence remains mandatory in its listed later phase.

## P02 evidence

| Evidence ID | Scope | Command / procedure | Expected result | Recorded result |
|---|---|---|---|---|
| P02-E001 | Complete textual-contract and traceability gate | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p02_quality.py`; Python unittest discovery | Exact full token/screen/matrix hashes, exact per-screen required-state mapping and exact 90/215 ledgers; equal-count drift rejected | PASS: 434/90/215/646/192 retained; complete hash gates pass; 13/13 total Python tests pass including equal-count state, non-sampled token and requirement-content mutations |
| P02-E002 | Frozen JVM/network/Android test infrastructure | Fresh aggregate JVM tests plus four GMD tasks | JUnit/Kotest/MockK/coroutines/Turbine/MockWebServer execute; Compose/Espresso, MigrationTestHelper and benchmark/profile harnesses execute | PASS: build logic 12/12; core testing 2/2; network 1/1; four device suites 4/4 |
| P02-E003 | Static architecture, privacy and write-gate policy | Fresh `detekt`, `spotlessCheck`, `verifySourcePolicies` and build-logic tests | Alias/type/scope-aware DAO writes; wrapper/alias-aware route/SavedState/telemetry/logging; full domain imports; all production source passes | PASS: detekt/Spotless/source gates exit 0; 12/12 policy engine tests pass |
| P02-E004 | Production-shaped rejection proof | Four proof wrappers over committed source and init-script fixtures | Every named fixture must fail due its own target diagnostic, including DAO alias, decoy Coordinator, route/state wrapper, telemetry/log alias and domain dependency | PASS: 14 source rule classes + 9 named P02 fixtures, 4 named money fixtures, domain-OkHttp init and missing-lock init all rejected |
| P02-E005 | Fresh aggregate quality gate | Same no-cache 2,110-task `p03Check` command as `P01-E006` | P01/P02 assemble/lint/test/detekt/Spotless/source/spec gates all execute, with failures propagated | PASS in 44s: 2,110/2,110 tasks executed |
| P02-E006 | Coverage and supply-chain artifacts under strict verification | `./gradlew p02Artifacts --configuration-cache --no-parallel --console=plain`; inspect generated JSON/XML/CSV/HTML | Normal mode, with dependency verification and locking active, generates aggregate Kover, CycloneDX and OSS inventory | PASS: 754 tasks; Kover XML/HTML; CycloneDX 1.6 JSON/XML with 736 components and 737 dependency nodes; license CSV/HTML with 736 rows (41 upstream metadata entries marked `UNKNOWN`); 37 lockfiles; root verification metadata has 1,133 components / 2,339 SHA-256 entries |
| P02-E007 | CI and callable API/performance entries | Parse `.github/workflows/quality.yml` with PyYAML; `./gradlew :app:tasks :benchmark:tasks :core:database:tasks --all --console=plain` | CI contains aggregate, all rejection proofs, artifact upload, API 28/API 36 GMD matrix and the API 36 MigrationTestHelper device task | LOCAL PASS: workflow/tasks are callable; remote Actions remains `UNVERIFIED` because no run URL/run ID/artifact reference is recorded |
| P02-E008 | Required local Managed Device execution | Fresh `--rerun-tasks --no-build-cache` API 28 app, API 36 app, API 36 benchmark and API 36 core:database GMD tasks | Compose/Espresso, performance-toolchain and MigrationTestHelper infrastructure execute on Emulator/KVM | PASS: four XML suites each 1/1 test; total 4 tests, 0 failures/errors/skipped |
| P02-E009 | Frozen-source and patch hygiene regression | Four Python validators; explicit ten-text frozen diff; `git diff --check`; `.pyc` scan | Frozen text unchanged; exact baselines coexist; no patch/cache defect | PASS: all validators and hygiene checks exit 0; 1,133 verification components / 2,339 SHA-256 entries; zero forbidden/runtime-prerelease/placeholder/cache findings |

P02 is `VERIFIED` for the repeatable local infrastructure and required local accelerated device entries only. Remote GitHub Actions execution remains `UNVERIFIED`; the workflow does not claim a remote pass. The 41 `UNKNOWN` upstream license metadata rows are retained visibly and do not satisfy or promote P36 NOTICE/release compliance.

## P03 evidence

Environment: Fedora host, Temurin JDK 17.0.20, Gradle 9.5.1, Kotlin 2.4.10, Asia/Tokyo. P03 production and behavioral tests execute only the pure Kotlin/JVM `:core:common`, `:core:money` and `:core:time` modules; they have no Android, Room or network dependency. Device evidence is neither required nor substituted for any platform behavior in this stage.

| Evidence ID | Scope | Command / procedure | Expected result | Recorded result |
|---|---|---|---|---|
| P03-E001 | Frozen baseline and complete P03 textual-contract query | Four Python validators with `PYTHONDONTWRITEBYTECODE=1` | Frozen text intact; exact 434/90/215/646 contracts, exact ledgers and five P03 mappings retained | PASS: all four validators exit 0; textual inputs are explicit allowlisted paths and visual drafts are excluded |
| P03-E002 | IDs, immutable results, typed errors and checked arithmetic | Fresh `:core:common:test`; inspect JUnit XML | Stable/Internal/Command/Revision typed IDs; checked add/subtract/multiply/negate/abs/accumulate and BigInteger fallback | PASS: 8/8 tests; addition and abs each use 1,000-case BigInteger oracle; `Long.MIN_VALUE` abs returns typed `ABS` overflow |
| P03-E003 | Money, legal-tender metadata, exact rounding, FX evidence/conversion and formatted UI model | `./gradlew :core:money:test --configuration-cache --no-parallel --console=plain`; inspect `MoneyAndCurrencyPropertyTest` and `FxAndFormattingTest` XML | Long minor units and per-currency scale; no crypto; checked sums; explicit rounding/MathContext; immutable quoted/fetched/provider/source/manual evidence; visible and privacy-safe hidden formatted models | PASS: 7/7 non-expression money/FX/formatting tests, zero failed/error/skipped; 1,000 generated Money additions; JPY/USD/KWD scales, overflow, currency mismatch, FX rounding and hidden-value non-leak pass |
| P03-E004 | Bounded deterministic amount expression language | Fresh `:core:money:test`; inspect `MoneyExpressionPropertyTest` XML | Required legal/illegal/full-width/locale/position/limit/extreme/rounding matrix | PASS: 11/11 expression tests; includes `÷`, isolated `)`, trailing operator, variable, scientific notation, `NUMBER_TOO_LONG`, token cap, extreme intermediate result and KWD rounding; money module 18/18 total |
| P03-E005 | Clock, effective time, storage keys and periods | Fresh `:core:time:test`; inspect JUnit XML | Gap defaults to reject; explicit shift carries adjustment provenance; overlap deterministic; all prior calendar/zone boundaries retained | PASS: 10/10 tests; default New York gap rejection and explicit 3,600-second adjustment evidence verified |
| P03-E006 | Pure-Kotlin and authoritative-money static rejection | Fresh build-logic tests, source gate, P03 validator and named fixture proof | No Float/Double or unchecked sum/fold/reduce/`+=`/manual Long accumulation; all named bypasses rejected | PASS: build logic 12/12; production policy clean; 4 named money fixtures rejected by binary-float/sum/accumulation rules |
| P03-E007 | Fresh inherited aggregate quality gate | No-cache 2,110-task `p03Check` command | Every inherited P00—P03 build/lint/test/spec/static gate executes | PASS in 44s: 2,110/2,110 tasks executed; configuration cache stored |
| P03-E008 | Coverage and inherited supply-chain artifacts | Fresh no-cache `p03Artifacts`; parse Kover XML, CycloneDX JSON and license CSV | Generate current P03-inclusive Kover, SBOM and OSS inventory | PASS: Kover 688/763 lines and 4,279/4,875 instructions; SBOM 736 components/737 dependency nodes; licenses 736 rows/41 visible UNKNOWN |
| P03-E009 | Final frozen-source, ledger and patch hygiene | Four Python validators; explicit allowed-text frozen diff; `git diff --check`; production placeholder/Python-cache scans; remediation tracker completeness check | All gates pass after ledger promotion; frozen text intact; no placeholder/cache/patch defect; all 215 screens remain unstarted | PASS: validators and hygiene checks exit 0; tracker 20/20 fixed; exact five P03 end-to-end rows remain `IN_PROGRESS`; INV-034/P00—P03 are truthfully `VERIFIED`; P04+ remains unstarted |

P03 is `VERIFIED`: exact typed ID/money/FX/expression/time/formatting foundations, checked abs, explicit DST-gap semantics and complete INV-034 static/property gates pass. This does not claim transaction persistence, offline FX repositories, financial planners or any Compose page; those remain P04 and later phases.

## P04 evidence

Environment: the same Fedora/JDK/Gradle/Android baseline above. Device cases run on locally accelerated Gradle Managed Devices: Pixel 2 API 28 Google APIs x86 and Pixel 6 API 36 Google APIs x86_64. P04 device tests exercise Compose rendering/semantics only; they do not substitute for later MapLibre, Glance, security or persistence device suites.

| Evidence ID | Scope | Command / procedure | Expected result | Recorded result |
|---|---|---|---|---|
| P04-E001 | Complete textual token/screen generation and mutation gates | `python3 scripts/generate_p04_contracts.py --check`; `python3 -m unittest scripts.tests.test_p04_ui_contracts -v`; inherited spec validators | Exact 434 scalar tokens, 215 unique routes, 646 states and target REQ rows; any unsampled token, unsafe route type, missing state or missing REQ changes/fails generation | PASS: 434/215/646; 4/4 P04 negative tests and inherited complete-hash gates pass |
| P04-E002 | Typed theme, contrast, dynamic boundary and chart compatibility | `./gradlew :core:designsystem:testDebugUnitTest`; inspect `LedgerTokenContractTest` XML | Typed values equal generated JSON; 16 category pairs, all semantic/chart cases and dynamic boundary pass contrast; pie 6/7 behavior deterministic | PASS: 8/8 tests, zero failures/errors/skips; generated token canonical SHA-256 retained |
| P04-E003 | Governed design-system API and Preview/localization foundation | Compile debug/main/androidTest; `python3 scripts/validate_p04_ui.py`; inspect 44 required component functions, closed models, resources and previews | All core wrappers/models/states, Vico/Map/Glance contracts, icon registry, zh/ja/en keys and 320/360/480 + 100/130/200% previews are present | PASS: complete source/contract validator and Android test compilation pass; no feature page or external SDK behavior claimed |
| P04-E004 | Navigation 3 route contract and five stacks | `./gradlew :core:navigation:testDebugUnitTest`; inspect `NavigationContractTest` XML | All YAML routes instantiate through typed arguments; no placeholders/raw public string payloads; five stacks, reselection, SessionGate and current-stack back pass | PASS: 5/5 tests, 215 IDs/routes and 646 required states, zero failures/errors/skips |
| P04-E005 | UI/privacy static governance and real rejection proof | `./gradlew :build-logic:test verifySourcePolicies`; `python3 scripts/prove_source_policy_rejection.py` | Production clean; feature Material/color/dp/theme/icon/swipe/duplicate/test-tag bypasses fail, with inherited privacy/write rules | PASS: 14/14 build-logic tests; 25 production files/215 screen IDs clean; 18 rule classes and 9 named fixtures rejected |
| P04-E006 | Formatting and static analysis | `./gradlew spotlessCheck detekt --no-configuration-cache --no-parallel --console=plain` | No ktlint/Detekt finding in production, test, preview, navigation or generator-support code | PASS, exit 0; generated sources are explicit deterministic formatting exclusions and remain hash-checked |
| P04-E007 | API 28 UI/golden/accessibility matrix | `./gradlew :core:designsystem:pixel2Api28DebugAndroidTest --no-configuration-cache --no-parallel --console=plain` | Token golden, hidden-value semantics, 48dp, size/font/theme/reduced-motion, map fallback and three locales render | PASS: 6/6 on Pixel 2 API 28, zero failures/errors/skips |
| P04-E008 | API 36 UI/golden/accessibility matrix | `./gradlew :core:designsystem:pixel6Api36DebugAndroidTest --no-configuration-cache --no-parallel --console=plain` | Same six cases on current target API, including token alpha-composition tolerance of at most one channel level | PASS: 6/6 on Pixel 6 API 36, zero failures/errors/skips |
| P04-E009 | Fresh cumulative P00—P04 aggregate | `./gradlew p04Check --configuration-cache --no-parallel --dependency-verification=strict --rerun-tasks --no-build-cache --console=plain` | All inherited assembly/lint/unit/static/spec gates plus P04 generation, design and navigation gates execute | PASS in 29s: 2,126/2,126 actionable tasks executed; configuration cache stored |
| P04-E010 | P04-inclusive auditable artifacts | `./gradlew p04Artifacts --configuration-cache --no-parallel --dependency-verification=strict --console=plain` after recording new preview POM SHA-256 metadata | Current Kover, CycloneDX and OSS license reports generate under strict locks/verification | PASS: full generation in 1m55s and normal strict replay in 8s (775 tasks); Kover XML/HTML (5,849/7,329 lines covered); CycloneDX 742 components/743 dependency nodes; license CSV/HTML 742 rows with 41 visible UNKNOWN entries |
| P04-E011 | Final ledger/frozen-source/worktree hygiene | P00/P01/P02/P03/P04 validators; generator `--check`; 17 Python mutation tests; explicit frozen-text diff; `git diff --check`; production placeholder/Python-cache scans; `spotlessCheck detekt verifySourcePolicies` | All ledgers match actual results; frozen specifications unchanged; 215 screens remain unstarted; no patch/cache/placeholder defect | PASS: all five validators, generator, 17/17 mutation tests and Gradle static gates passed on 2026-08-01; P01 reports 37 lockfiles, 1,143 verification components and 2,361 SHA-256 entries; frozen-text/diff checks exit 0; production placeholder and `.pyc` findings are zero; all 215 feature screens remain `NOT_STARTED` and P05+ is unstarted |

P04 verification is limited to the cross-cutting design, navigation and test contracts. It does not claim any of the 215 feature screen implementations, external MapLibre/Glance capability, database persistence, or completion of later accessibility acceptance across feature pages.

## P05 evidence

Environment: the same Fedora/Temurin JDK 17.0.20/Gradle 9.5.1/Kotlin 2.4.10 baseline above. P05 production and behavior tests execute only the pure Kotlin/JVM `:finance:domain`, `:finance:application`, `:analytics:domain` and `:transfer:domain` modules; none depends on Android, Room, Hilt, OkHttp or UI. P05 asserts no SQLCipher, Keystore, biometric, SAF, location, Worker/UIDT or device behavior, so no device-only claim is replaced by a JVM test.

| Evidence ID | Scope | Command / procedure | Expected result | Recorded result |
|---|---|---|---|---|
| P05-E001 | Exact §1—§35 model/port mapping and closed source contract | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p05_domain.py`; inspect `P05_DOMAIN_API_MAPPING.md` | Complete aggregate/lifecycle/query/operation declarations, exact 11 transaction payloads, six lifecycle types, 41 tracked REQs and 215 untouched screens; Android/framework/property-bag/placeholder use rejected | PASS: 19 production files, 229 finance declarations, 19 required application ports, 11/11 payloads, 6/6 lifecycles, exact 35 mapping rows and 41 requirement foundations; 215/215 screens remain `NOT_STARTED` |
| P05-E002 | Domain, coordinator, analytics and transfer behavior | Fresh `:finance:domain:test :finance:application:test :analytics:domain:test :transfer:domain:test`; parse JUnit XML | Type closure, accounting/subledger policies, mutation lifecycles, idempotent atomic coordinator, 20-report AST, operation state and merge/purge policies pass | PASS: finance domain 18/18, finance application 3/3, analytics 4/4, transfer 4/4; total 29 tests, zero failure/error/skipped; Journal/budget/settlement properties exercise 3,000 generated cases |
| P05-E003 | Compile-time shape, pure architecture and real rejection gates | `./gradlew :build-logic:test verifyArchitecture verifySourcePolicies detekt spotlessCheck`; `python3 scripts/prove_source_policy_rejection.py`; `python3 -m unittest scripts.tests.test_p05_domain_contracts -v` | Compiled public types structurally require category/payer/single project/goal and currency-checked account amounts; domain has no forbidden dependency/generic payload; features cannot access DAO/Entity; weakening mutations fail | PASS: build logic 15/15; architecture 40 projects/edges; source policy clean over 44 production files/215 IDs; 19 rule classes and 10 named fixtures rejected; 5/5 P05 mutation tests pass; Detekt/Spotless pass |
| P05-E004 | Fresh cumulative P00—P05 aggregate | `./gradlew p05Check --configuration-cache --no-parallel --dependency-verification=strict --rerun-tasks --no-build-cache --console=plain` after correcting inherited validators to accept a retained earlier VERIFIED stage | Every inherited assembly/lint/unit/static/spec/generation gate plus all P05 domain checks executes without cache reuse | PASS in 25s: 2,139/2,139 actionable tasks executed; configuration cache stored |
| P05-E005 | P05-inclusive coverage and supply-chain artifacts | `./gradlew p05Artifacts --configuration-cache --no-parallel --dependency-verification=strict --console=plain`; parse Kover XML, CycloneDX JSON and license CSV | Current Kover, SBOM and OSS inventory include P05 modules and retain strict dependency verification | PASS in 28s: 787 tasks (140 executed, 647 up-to-date); Kover 6,696/10,379 lines and 27,676/67,287 instructions; CycloneDX 742 components/743 dependency nodes; licenses 742 rows with 41 visible `UNKNOWN` entries |
| P05-E006 | Final inherited-contract, ledger and patch hygiene | P00—P05 validators; generator `--check`; all Python mutation tests; explicit frozen-text diff; `git diff --check`; production placeholder/framework/generic-payload/Python-cache scans | Frozen textual specifications unchanged, no forbidden visual input, all ledgers match execution, no patch/cache/placeholder defect and no later-stage promotion | PASS on 2026-08-01: all validators and 22/22 Python mutation tests pass; frozen-text/diff/scans exit 0; P05 remains a pure model/port stage, its 41 requirement rows remain `IN_PROGRESS`, and all 215 screens remain `NOT_STARTED` |

P05 is `VERIFIED` for the complete pure domain model and application-port boundary. It does not claim the P06 rule-by-rule accounting planners, P07/P08 schema/projection persistence, adapter behavior, any feature screen, or any platform/device behavior.

## P06 evidence

Environment: the same Fedora/Temurin JDK 17.0.20/Gradle 9.5.1/Kotlin 2.4.10 baseline. P06 runs only pure Kotlin/JVM finance modules and inherited static/build gates. It makes no SQLCipher, Room, Keystore, biometric, SAF, location, foreground/UIDT or device claim, so no device-only behavior is inferred from JVM evidence.

| Evidence ID | Scope | Command / procedure | Expected result | Recorded result |
|---|---|---|---|---|
| P06-E001 | Closed accounting rules, invariant mapping and mutation resistance | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p06_accounting.py`; `python3 -m unittest scripts.tests.test_p06_accounting_contracts -v`; inspect `P06_ACCOUNTING_INVARIANT_MAPPING.md` | Exactly 11 frozen rules, five planner/hash/fact files, 35 invariant rows and 25 P06 accounting-core automated mappings; removed rule, clock, floating money, missing effect hash or canonical coordinator check must fail | PASS: validator reports 19 finance production files, 11 rules, 9 target requirements, 35 invariant rows, 25 accounting-core automated mappings and 215 untouched screens; 5/5 named mutations rejected |
| P06-E002 | Deterministic planner, lifecycle, effects and coordinator behavior | Fresh `:finance:domain:test :finance:application:test`; parse JUnit XML | Balanced rules, 2,500 generated cases, edit/trash/restore equivalence, FX rounding/extremes/time zones, candidates, dependencies, hash/idempotency/conflicts and journal-less settlement lifecycle pass | PASS: finance domain 38/38 and application 4/4, zero failure/error/skipped; 1,000 expenses + 1,000 transfers + 500 trash reversals generated; 16 focused planner cases cover every rule, all credit-account statement paths and high-risk boundaries |
| P06-E003 | Pure architecture, money/privacy/write static gates and formatting | `./gradlew :build-logic:test verifyArchitecture verifySourcePolicies detekt spotlessCheck`; `python3 scripts/prove_source_policy_rejection.py` | Domain remains Android/Room/Hilt/OkHttp/UI-free; no floating/unchecked money, clock/random, placeholder, DAO bypass or sensitive output; named policy fixtures still fail | PASS: build logic 15/15; architecture 40 projects/edges; 49 production files/215 IDs clean; Detekt/Spotless pass; 19 policy classes and 10 named fixtures reject every intended bypass |
| P06-E004 | Fresh cumulative P00—P06 aggregate | `./gradlew p06Check --configuration-cache --no-parallel --dependency-verification=strict --rerun-tasks --no-build-cache --console=plain` | Every inherited assembly/lint/unit/static/spec/generation/P05 gate plus P06 validator and finance tests executes without cache reuse | PASS in 23s: 2,140/2,140 actionable tasks executed; configuration cache stored; P03/P04/P05/P06 validators and all inherited assembly/lint/tests pass |
| P06-E005 | P06-inclusive coverage and supply-chain artifacts | `./gradlew p06Artifacts --configuration-cache --no-parallel --dependency-verification=strict --console=plain`; parse Kover XML, CycloneDX JSON and license CSV | Current coverage, SBOM and OSS inventory include P06 while strict locks/verification remain active | PASS in 1s after final tests: 783 tasks (10 executed, 773 up-to-date); Kover XML/HTML has 9,172/12,476 lines and 40,592/78,834 instructions covered; CycloneDX 1.6 has 742 components/743 dependency nodes; licenses 742 rows with 41 visible `UNKNOWN` entries |
| P06-E006 | Final frozen-source, ledger and patch hygiene | P00—P06 validators; P04 generator `--check`; all Python mutation tests; explicit ten-text frozen diff; `git diff --check`; production placeholder/framework/float/nondeterminism/Python-cache scans | Frozen textual specifications unchanged, excluded visuals untouched, ledgers match execution, no patch/cache/placeholder defect and no P07+ promotion | PASS on 2026-08-02: all seven stage validators, generator and 27/27 Python mutation tests pass; exact 10-text frozen diff and patch/cache/scans are clean; P01 retains 37 lockfiles/1,143 verification components/2,361 hashes; 9 P06 REQs remain `IN_PROGRESS`, all 215 screens remain `NOT_STARTED`, and P07+ remains unstarted |

P06 verification is limited to the deterministic pure accounting kernel and its application command boundary. Physical immutability/uniqueness, SQLCipher atomicity, projection rebuilds, report/UI acceptance and platform behavior remain P07/P08 and later.

## P07 evidence

Environment: Fedora, Temurin JDK 17.0.20, Gradle 9.5.1, AGP 9.3.1, Kotlin 2.4.10, Android SDK/target 36 and Asia/Tokyo. Device evidence runs on the locally accelerated Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device with SQLCipher's official x86_64 native library. It proves encrypted SQLite behavior only; Keystore key derivation, repository transactions, projection rebuild algorithms and later platform integrations are not inferred.

| Evidence ID | Scope | Command / procedure | Expected result | Recorded result |
|---|---|---|---|---|
| P07-E001 | Exact frozen Schema v1 mapping and deterministic exports | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/generate_p07_schema_catalog.py --check`; `python3 scripts/validate_p07_database.py`; inspect `P07_SCHEMA_MAPPING.md`, Room JSON and canonical DDL JSON | Exact 94 frozen §25 tables remain a subset; explicit rule version, all projections/operations/virtual indexes, fields, constraints and typed subtype tables are present in independently versioned primary/staging schemas | PASS: 94 frozen tables map field-by-field into 140 primary declarations; seven staging tables, 39 named indexes, four views and 63 append-only tables are deterministic and generated catalogs match source assets |
| P07-E002 | JVM migration contract and fail-closed mutation suite | Fresh `:core:database:testDebugUnitTest`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p07_database_contracts -v`; inspect JUnit XML | v1 empty-predecessor registration is explicit; future migrations are adjacent and Expand → Backfill → Switch → Contract ordered; missing table/FTS5, JSON, `@Upsert`, destructive fallback, file temp storage and inherited-stage regression mutations fail | PASS: database JVM suite 4/4 and P07 named mutation suite 7/7, zero failure/error/skipped |
| P07-E003 | Actual-device SQLCipher schema, capability, integrity and leakage validation | `./gradlew :core:database:pixel6Api36DebugAndroidTest --configuration-cache --no-parallel --dependency-verification=strict --console=plain`; inspect managed-device XML | Create/close/reopen both databases, wrong-key rejection, SQLCipher 4.17.0, FTS5/R*Tree/JSON/window support, WAL/secure pragmas, integrity/FK/audit views, immutable rejection and plaintext side-file scan pass | PASS in the final 28s cumulative replay: 4/4 tests on Pixel 6 API 36, zero failure/error/skipped; the test uses the production-fixed `ledger.db` name, main and staging correct-key reopen, wrong-key failure, all capability/audit queries and the sensitive-sentinel database/WAL scan pass |
| P07-E004 | Static governance and cumulative quality gate | `./gradlew p07Check --configuration-cache --no-parallel --dependency-verification=strict --rerun-tasks --no-build-cache --console=plain`; focused `detekt spotlessCheck verifySourcePolicies verifyArchitecture` | All inherited P00—P06 gates plus P07 generation, schema, migration, JVM and static policies execute without cache reuse; feature code still cannot access DAO/Entity or bypass the coordinator | PASS in 25s: 2,151/2,151 actionable tasks executed; all cumulative validators, assembly, Lint, Detekt, Spotless, architecture/source policies and JVM suites pass; the inherited P06 validator now has a tested cumulative-stage guard instead of rejecting a valid P07 ledger |
| P07-E005 | P07-inclusive coverage and supply-chain artifacts | `./gradlew p07Artifacts --configuration-cache --no-parallel --dependency-verification=strict --console=plain`; parse Kover XML, CycloneDX JSON and license CSV | Strict verification includes SQLCipher AAR/module/POM and current coverage/SBOM/license artifacts generate | PASS: first full generation after recording the SQLCipher 4.17.0 POM SHA-256 completed in 2m17s (795 tasks); strict no-write replay passed in 10s; Kover 9,268/12,876 lines and 41,042/81,212 instructions; CycloneDX 1.6 has 763 components/764 dependency nodes; licenses have 763 rows with 42 visible `UNKNOWN` entries |
| P07-E006 | Final frozen-source, ledger and patch hygiene | P00—P07 validators; P04/P07 generators; all Python mutation tests; explicit ten-text frozen diff; `git diff --check`; production placeholder/Python-cache/forbidden-draft-access scans | Frozen textual specifications unchanged, excluded visuals unread, ledgers match execution, no patch/cache/placeholder defect, all 215 screens remain unstarted and P08+ is not promoted | PASS on 2026-08-02: all ten cumulative validator/generator commands and 34/34 Python mutation tests pass; source-policy proof rejects 19 rule classes/10 named fixtures; current baseline reports 37 lockfiles, 1,168 verification components and 2,417 SHA-256 entries; exact frozen-text diff, patch, production placeholder and Python-cache checks are clean; all 215 screens remain `NOT_STARTED`, P08+ remains unstarted, and no excluded PNG/HTML visual draft was opened or used for a baseline |

P07 verification is limited to encrypted physical Schema v1, first-version migration governance, SQLite/SQLCipher capabilities and device leakage checks. Repository adapters, DAO-to-domain mapping, atomic coordinator persistence, projection rebuilds, production Keystore keys, feature screens and later operations remain P08 and their owning later phases.

## P08 evidence

Environment: Fedora, Temurin JDK 17.0.20, Gradle 9.5.1, AGP 9.3.1, Kotlin 2.4.10, Android SDK/target 36 and Asia/Tokyo. Device evidence runs on the locally accelerated Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device against the production Room-owned SQLCipher connection. P08 makes no Keystore, biometric, SAF, network, foreground/UIDT, feature UI, target-scale performance or later projection claim.

| Evidence ID | Scope | Command / procedure | Expected result | Recorded result |
|---|---|---|---|---|
| P08-E001 | Exact application/data/projection/query mapping and mutation resistance | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p08_data.py`; `python3 -m unittest scripts.tests.test_p08_data_contracts -v`; inspect `P08_REPOSITORY_PROJECTION_MAPPING.md` | One typed command entry, complete normalized writer, 19 synchronous projection tables, keyset/FTS/R*Tree query base, maintenance/audit, seven target REQs and 215 untouched screens; transaction/projection/paging/freshness weakening mutations fail | PASS: source/test-only validator covers 19 production files and 19 synchronous derived tables; 4/4 named mutations reject transaction-boundary removal, missing projection, offset paging and false freshness stamping; final ledger-inclusive replay is included in `P08-E004` |
| P08-E002 | Application gate, coordinator, typed SQL compiler and static ownership | `./gradlew :build-logic:test :finance:application:test :finance:data:testDebugUnitTest verifySourcePolicies verifyArchitecture detekt spotlessCheck --no-configuration-cache --no-parallel --dependency-verification=strict --console=plain`; inspect JUnit XML | concurrent submissions serialize; canonical/idempotent coordinator behavior remains; SQL predicates are bound and keyset-only; feature/Worker/importer cannot obtain DAO/Entity/planning/atomic ports or mutate financial SQL | PASS: focused application 5/5, data JVM 2/2 and build logic 17/17 passed; static policies reject privileged application ports, DAO/Entity exposure, direct financial SQL and coordinator bypasses; final replay is included in `P08-E004` |
| P08-E003 | Actual-device SQLCipher atomicity, projections, audit/rebuild and query behavior | `./gradlew :core:database:pixel6Api36DebugAndroidTest :finance:data:pixel6Api36DebugAndroidTest --no-configuration-cache --no-parallel --dependency-verification=strict --console=plain`; inspect managed-device XML | P07 encryption/capability regression plus duplicate command, stale expected revision, five transaction fault phases, local/valuation versions, keyset, FTS, R*Tree/Haversine, corruption detection and exact rebuild all pass | PASS in 53s: core database 4/4 and finance data 2/2 on Pixel 6 API 36, zero failure/error/skipped; two real expense commits persist complete normalized facts/effects, duplicate writes remain at two, every fault phase leaves only genesis state, and rebuild restores the original canonical hash |
| P08-E004 | Fresh cumulative P00—P08 aggregate | `./gradlew p08Check --configuration-cache --no-parallel --dependency-verification=strict --rerun-tasks --no-build-cache --console=plain` | Every inherited P00—P07 build/lint/unit/static/spec/schema gate plus P08 application/data/build-logic/ledger validation executes without cache reuse | PASS in 26s: 2,159/2,159 actionable tasks executed with build cache disabled; assemble debug/release, Android Lint, Detekt, Spotless, architecture/source policies, all JVM tests and every P00—P08 validator/generator completed successfully under strict dependency verification |
| P08-E005 | P08-inclusive auditable artifacts | `./gradlew p08Artifacts --configuration-cache --no-parallel --dependency-verification=strict --console=plain`; parse Kover XML, CycloneDX JSON and license CSV | Current coverage, SBOM and OSS inventory include the P08 modules while strict locks and dependency verification remain active | PASS in 29s: 801 tasks (140 executed, 661 up-to-date); Kover XML/HTML generated with 9,343/14,115 lines and 1,303/4,277 branches covered; CycloneDX 1.6 JSON contains 763 components and 764 dependency nodes; license CSV contains the same 763 components (42 unknown identifiers retained for later release adjudication); 37 lockfiles and 1,168 strictly verified components remain present |
| P08-E006 | Final frozen-source, ledger and patch hygiene | P00—P08 validators; P04/P07 generators; all Python mutation suites; explicit ten-text frozen diff; `git diff --check`; production placeholder/forbidden-framework/Python-cache/excluded-visual scans | Frozen specifications unchanged, excluded visual drafts unread, ledgers match execution, no patch/cache/placeholder defect, 215 screens remain unstarted and P09+ is not promoted | PASS: final independent audit reran all validators/generators and 39/39 mutation tests, source-policy rejection proof, exact frozen-text baseline, patch whitespace and production-placeholder scans; no frozen source or excluded visual draft is changed or used, no Python cache remains, all 215 screens are still `NOT_STARTED`, and P09 remains unstarted |

P08 verification is limited to the unique financial write boundary, normalized atomic persistence, synchronous derived-state reconstruction and typed query foundation. Production key management, feature screens, background/import execution, full analytics/valuation/future projections, widget runtime, merge/purge and target-scale performance remain their owning later stages.

## P09 evidence

Environment: Fedora, Temurin JDK 17.0.20, Gradle 9.5.1, AGP 9.3.1, Kotlin 2.4.10, Android SDK/target 36 and Asia/Tokyo. Platform evidence runs on the locally accelerated Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device. JVM tests prove only pure primitives/state machines; Android Keystore, device credential, `BiometricPrompt.CryptoObject` and SQLCipher behavior are proved by Android device tests, not Robolectric.

| Evidence ID | Scope | Command / procedure | Expected result | Recorded result |
|---|---|---|---|---|
| P09-E001 | Tink/Argon2id/AAD, secret lifetime, app lock and session-state behavior | `./gradlew :core:security:testDebugUnitTest --dependency-verification=strict`; parse JUnit XML | AEAD and Streaming AEAD reject wrong AAD; recovery has no alternate password path; bounded plaintext clears; all lock timeouts and UI/headless/maintenance/recovery state transitions pass | PASS: 13/13 JVM tests, zero failure/error/skipped; 1.2 MB streaming round-trip, 64 MiB/three-iteration Argon2id wrap/unwrap/wrong-password/schema-binding, foreground/background/app-lock secret closure, session ref-count, maintenance preservation and headless inspection/exception-close cases pass |
| P09-E002 | Security contract/static mutation resistance | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p09_security_contracts -v`; `./gradlew :build-logic:test verifySourcePolicies spotlessCheck detekt` | Device KEK auth regression, vault auth window, biometric enrollment dropping the required credential fallback, pre-auth AAD, database-bearing headless lease, security-code copy, raw secret and feature capability imports are rejected | PASS: 6/6 P09 validator mutations and 18/18 build-logic tests pass; source policies pass over 73 production files/215 screen IDs; the production-wide Detekt/Spotless replay passes |
| P09-E003 | Actual-device DeviceLedgerKEK, SQLCipher session and screen privacy | `./gradlew :core:security:pixel6Api36DebugAndroidTest --no-configuration-cache --no-parallel --dependency-verification=strict --console=plain`; inspect managed-device XML | Device KEK requires no user authentication; production SQLCipher opens/closes/reopens; locked headless lease remains opaque; deleted key cannot regenerate over an envelope and requires recovery; vault/background/global policies drive the real window secure flag | PASS on Pixel 6 API 36 in 29s: 5/5 device tests, zero failure/error/skipped; device-key/session and `FLAG_SECURE` cases use real Android Keystore, production Room/SQLCipher connection and Activity window |
| P09-E004 | Actual-device auth-per-use vault and device-security change | Same API 36 managed-device command; `VaultAuthenticationDeviceTest` temporarily sets a test PIN, authenticates two separate CryptoObjects, verifies credential-only availability, zero-second combined strong-biometric/device-credential key policy and the sanitized no-biometric disposition, clears the credential and checks failure | Provisioning and recovery export each require fresh real system authentication; post-auth AAD succeeds; biometric unavailability does not leak framework details; cleared device credential prevents vault reuse | PASS after device-driven repair: an initial run exposed pre-auth `updateAAD` as `KEY_USER_NOT_AUTHENTICATED`, and a later run exposed the false enrollment-invalidation assumption. The final 5/5 API 36 result verifies credential-only detection, `invalidatedByBiometricEnrollment=false` as required for fallback, both authenticator bits, two independent PIN-authenticated CryptoObjects and failure after PIN removal; teardown clears the PIN |
| P09-E005 | Fresh cumulative P00—P09 aggregate and dependency governance | `./gradlew p09Check --configuration-cache --no-parallel --dependency-verification=strict --rerun-tasks --no-build-cache --console=plain` | Every inherited P00—P08 build/lint/unit/static/spec/schema gate plus P09 validator/security tests executes without cache reuse; strict locks and verification cover Tink/Bouncy Castle/Biometric | PASS in 30s: 2,167/2,167 actionable tasks executed with build cache disabled; all assemblies, Lint, Detekt, Spotless, architecture/source policies, JVM suites and P00—P09 validators/generators pass; 37 lockfiles and strict verification cover 1,196 components/2,471 SHA-256 entries |
| P09-E006 | Artifacts, frozen-source and final repository hygiene | `./gradlew p09Artifacts --configuration-cache --no-parallel --dependency-verification=strict --console=plain`; all validators/mutations; ten-text frozen diff; `git diff --check`; placeholder/cache/forbidden-visual audits | Current Kover/SBOM/license artifacts generate; frozen specs and excluded visual inputs remain untouched; no placeholder/cache defect; screens remain unstarted and P10+ unpromoted | PASS: current generation finished in 2s (803 tasks, 12 executed) and strict read-only replay in 408ms (803 up-to-date); Kover covers 9,707/15,077 lines, 44,116/96,861 instructions and 1,418/4,720 branches; CycloneDX has 785 components/786 nodes and license CSV 785 rows/42 visible unknowns. All 12 validators/generators, 45/45 Python mutations, 19-rule/10-fixture rejection proof, frozen-text diff, patch/placeholder/cache audits pass; all 215 screens remain `NOT_STARTED`, P10+ remains unstarted, and no excluded PNG/HTML draft was opened or used |

## P10 evidence

Environment for `P10-E001`—`P10-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1; compile/target SDK 36; Pixel 6 x86_64 API 36 Google image under KVM 12; Room 2.8.4; SQLCipher 4.17.0; Tink 1.23.0; Coil 3.5.0; MapLibre 13.4.1; Play Services Location 21.3.0. Device suites ran serially with one managed-device worker.

| Evidence ID | Verification | Command / test | Acceptance | Result |
|---|---|---|---|---|
| P10-E001 | Machine-readable P10 source/dependency/state mapping and mutation resistance | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p10_files_geo.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p10_files_geo_contracts -v` | Streaming AEAD/hash+size/Provider/location/map/state contracts and exact dependency ownership pass; mutations for non-streaming crypto, reusable URI, >3-second wait, background location and missing map fallback fail | PASS: P10 validator exit 0; 5/5 P10 negative mutation tests pass; exact 18 required states across REC-009/010, ATT-001—003 and SYS-001 retained |
| P10-E002 | Metadata, progress, three-second session and map value JVM contracts | `./gradlew :core:files:testDebugUnitTest :core:geo:testDebugUnitTest --no-configuration-cache --max-workers=2 --dependency-verification=strict` | Filename/MIME/path normalization, bounded progress, one remaining monotonic wait, frozen location, checked E7, secure styles and attribution invariants | PASS: 6/6 JVM tests, zero failure/error/skipped |
| P10-E003 | Actual-device encrypted attachment object/Coil/Provider lifecycle | `./gradlew :core:files:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | Large streaming import/dedup/decrypt/rename/GC; cancellation/database/process failure safety; encrypted thumbnail; one-time pipe and lock; all ATT states at 320dp/200% | PASS on Pixel 6 API 36: 4/4 tests, zero failure/error/skipped; 5 MiB sentinel has no plaintext private-file occurrence and every catalog row retains an encrypted object |
| P10-E004 | Actual-device foreground location, permission and MapLibre lifecycle/fallback | `./gradlew :core:geo:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | Denial touches no provider; missing Play services uses LocationManager; deadline yields no later location; actual MapLibre renders cluster/heat/single with attribution and accessible fallback; SYS states render | PASS on Pixel 6 API 36: 5/5 tests, zero failure/error/skipped. An initial test-only runtime-permission revoke killed instrumentation as Android specifies; replacing that side effect with an injected permission context made the complete final suite stable without weakening production checks |
| P10-E005 | REC/ATT/SYS required-state UI, compact width, large font and privacy semantics | File ATT test above plus `./gradlew :feature:record:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | All 18 P10 required states render; REC high-risk combination uses 320dp/200%; failure states do not block saving; no physical path/hash/raw coordinate appears in semantics | PASS: ATT 1/1 and REC 2/2 device UI tests; zero failure/error/skipped; stable design-system tags and generic encrypted-image semantics used |
| P10-E006 | Static architecture/privacy, formatting, lint and dependency governance | `./gradlew verifyArchitecture verifySourcePolicies spotlessCheck detekt :core:files:lintDebug :core:geo:lintDebug :feature:record:lintDebug`; included build-logic tests | Feature SDK/infrastructure imports, background location, shared storage, sensitive route/state/logging and financial write bypasses are rejected; production clean | PASS: source policy scans 84 production files/215 screen IDs; 19/19 build-logic rule tests pass; Spotless, Detekt and three targeted Lint tasks exit 0 |
| P10-E007 | Fresh cumulative P00—P10/device gate, artifacts and final hygiene | `./gradlew p10Check --configuration-cache --no-parallel --max-workers=1 --dependency-verification=strict --console=plain`; `./gradlew p10Artifacts --configuration-cache --no-parallel --max-workers=1 --dependency-verification=strict --console=plain`; all baseline validators/mutations; frozen-text diff; `git diff --check`; placeholder/cache audit | Every inherited and P10 JVM/static/lint/three serial API 36 suites passes; artifacts generate under strict dependency verification; frozen text and excluded visual inputs remain untouched; no later stage promoted | PASS: cumulative gate finished in 3m37s with 2,296 actionable tasks (1,024 executed, 122 cache, 1,150 up-to-date), including attachment 4/4, geo 5/5 and record 2/2 device tests. Strict artifact replay passed with 822/822 tasks up-to-date; Kover covers 9,820/16,477 lines, 44,979/108,908 instructions and 1,491/5,547 branches; CycloneDX has 808 components/809 dependency nodes and the license CSV has 808 rows/42 visible unknowns. All 11 stage validators and 50/50 Python mutation tests pass; frozen source hashes are unchanged; no placeholder, Python cache or patch defect; four excluded visual drafts were never opened or used |

P10 verification is limited to encrypted attachment storage/presentation/sharing, foreground location capture, the MapLibre foundation and the exact P10-owned required states. The complete P13 record workflow, P27 analytics map screens, online place search/reverse geocoding, background location and later import/backup Workers remain their owning stages.

## P11 evidence

Environment for `P11-E001`—`P11-E008`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 2 API 28 Google APIs x86 and Pixel 6 API 36 Google APIs x86_64 Gradle Managed Devices under KVM. Device suites ran serially with one managed-device worker. The four P11 baselines were generated from the implemented Compose tree using only textual contracts/tokens and were never sourced from an excluded visual draft.

| Evidence ID | Verification | Command / test | Acceptance | Result |
|---|---|---|---|---|
| P11-E001 | Exact root/proto/dependency/YAML/ledger mapping and mutation resistance | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p11_app_shell.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p11_app_shell_contracts -v`; inspect `P11_APP_SHELL_MAPPING.md` | One root, typed safe settings/routes, exact 18 screens/65 required states, frozen golden hashes and P11 ledger dispositions pass; SessionGate/Ready gate/privacy/design-system weakening mutations fail | PASS: validator covers the complete P11 production/test/ledger surface; 5/5 named negative mutations reject missing SessionGate, pre-Ready navigation, recovery plaintext retention, sensitive Proto fields and a local Material theme |
| P11-E002 | JVM onboarding, navigation, restore and write-gate contracts | `./gradlew :app:testDebugUnitTest :feature:onboarding:testDebugUnitTest :core:navigation:testDebugUnitTest :finance:application:test --no-configuration-cache --max-workers=1 --dependency-verification=strict` | Exact ten-step order/optionality, safe restore policies, independent stacks/reselection, all destinations blocked before Ready and coordinator rejection outside Ready pass | PASS: app 3/3, onboarding 4/4, navigation 9/9 and finance application 7/7; total 23 tests, zero failure/error/skipped |
| P11-E003 | Actual-device single-root cold launch and encrypted empty-book initialization | `./gradlew :app:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; focused `:finance:data:pixel6Api36DebugAndroidTest` initialization class | Real Hilt Activity/DataStore/Keystore/SQLCipher runtime drives all ten steps, skips optional recovery/account/category, enters `REC-001`/`EXPENSE`, persists no sample financial facts and reopens an idempotent encrypted genesis | PASS on Pixel 6 API 36: app 7/7 including runtime 1/1 and finance initialization 1/1, zero failure/error/skipped; the book has zero transactions, Journals and Postings |
| P11-E004 | API 36 G/ONB state, layout, localization, accessibility and privacy matrix | App API 36 command above; `P11UiContractDeviceTest` | All 65 states, 320/360/480dp, 100/130/200% font, Simplified Chinese/Japanese/English, light/dark/dynamic boundary and sensitive semantic-tree exclusion pass | PASS: 4/4 P11 UI tests on Pixel 6 API 36, zero failure/error/skipped |
| P11-E005 | API 28 compatibility UI matrix | `./gradlew :app:pixel2Api28DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.app.P11UiContractDeviceTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | The same 65-state, width/font/theme/locale/privacy contract remains usable at minSdk | PASS: 4/4 on Pixel 2 API 28, zero failure/error/skipped |
| P11-E006 | Contract-origin screenshot/golden exactness | `P11GoldenDeviceTest` inside the API 36 app suite; `validate_p11_app_shell.py` verifies asset set/dimensions/SHA-256 | Four governed 360×720 Locked/Maintenance/Recovery/onboarding-error images match every pixel and cannot drift outside the machine-frozen hashes | PASS: 1/1 API 36 golden test, zero changed pixels; four PNG dimensions and SHA-256 values match the validator oracle |
| P11-E007 | Static architecture/privacy, formatting, lint and fresh cumulative gate | `./gradlew p11Check --no-configuration-cache --max-workers=1 --dependency-verification=strict --rerun-tasks --no-build-cache --console=plain`; included architecture/source-policy/Detekt/Spotless/Lint tasks | No second Activity/shell, DAO/Entity or financial write bypass, unsafe route/state, direct clock/random, local Material theme, hardcoded design value or placeholder; all inherited P00—P09 gates and P11 tests pass fresh | PASS in 2m33s: 2,252/2,252 tasks executed with build cache disabled; architecture covers 40 projects, source policy covers 96 production files/215 IDs, app/onboarding Lint, Detekt, Spotless, all JVM suites and API 36 app 7/7 pass. The bundled FIR/JDK-17 analyzer incompatibility was repaired without changing the frozen toolchain or suppressing Lint (`DL-055`) |
| P11-E008 | Artifacts, frozen-source, lock and final repository hygiene | `./gradlew p11Artifacts --configuration-cache --no-parallel --dependency-verification=strict --console=plain`; all P00—P11 validators/mutations; frozen-text diff; `git diff --check`; placeholder/cache audit | Current Kover/SBOM/license outputs include P11 dependencies under strict locks; frozen specifications remain unchanged; excluded drafts remain unread; no patch/cache/placeholder defect and no P12+ promotion | PASS: strict artifact replay passed after registering the new trusted POM SHA-256 values. Kover covers 10,283/20,694 lines, 47,305/135,549 instructions and 1,634/7,316 branches; CycloneDX has 840 components/841 dependency nodes and the license CSV has 840 rows/42 visible unknowns. P01 records 37 lockfiles, 1,357 verification components and 2,802 SHA-256 entries; all 12 stage validators and 55/55 Python mutation tests pass. Frozen source and excluded visual drafts remain unchanged and unread, no patch/cache/placeholder defect remains, and P12+ is `NOT_STARTED` |

P11 verification is limited to the single application shell, global session states, five Navigation 3 stacks, secure first launch and G/ONB contract surfaces. It does not claim later feature pages, complete settings, durable background operations, backup/restore transport, widgets, feature-wide accessibility/performance acceptance or later process-death form behavior.

## P12 evidence

Environment for `P12-E001`—`P12-E008`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 2 API 28 Google APIs x86 and Pixel 6 API 36 Google APIs x86_64 Gradle Managed Devices under KVM. SQLCipher/device tests use the real Android Keystore-backed P09 key provider. P12 created no screenshot baseline; the excluded visual PNG/HTML drafts were not opened, parsed, measured or used.

| Evidence ID | Verification | Command / test | Acceptance | Result |
|---|---|---|---|---|
| P12-E001 | Exact YAML/source/test/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p12_reference_data.py`; inspect `P12_REFERENCE_DATA_MAPPING.md` | Exact 23 screens/67 states, typed production paths, governed components, atomic batch rewrites and truthful ledgers pass; false P12 promotion is rejected | PASS after final ledger update: 23 screens, 67 states, two coordinator-owned atomic batch rewrites, `VERIFIED` screen rows and contract-only visual inputs |
| P12-E002 | Pure domain account/card/category/checkpoint and batch-edit policies | `./gradlew :finance:domain:test --no-configuration-cache --dependency-verification=strict`; inspect JUnit XML | Last/used account, first-Posting currency lock, closed card compatibility, immutable second-level parent, checked checkpoint difference and deterministic multi-edit REVERSE/APPLY planning pass | PASS: finance domain 44/44; P12 policy class 5/5 and deterministic planner class 17/17, zero failure/error/skipped |
| P12-E003 | SQLCipher/Room account and reference-data integration | `./gradlew :finance:data:pixel6Api36DebugAndroidTest :app:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | Four account types, card compatibility/replacement, coordinator opening balance, currency lock, checkpoint no-balance effect, category hierarchy/defaults/tombstone/reassignment, merchant/place merge/split, audit/revision and transaction rollback execute on device | PASS in the final 1m44s replay on Pixel 6 API 36: finance data 4/4, zero failure/error/skipped; two `BATCH_MUTATION` commits append three expense revisions and two unique reversals, move current category/location references, preserve old facts and pass `integrity_check` |
| P12-E004 | API 36 account/reference UI and root integration | Same final combined API 36 command as `P12-E003` | All 67 P12 states, representative projection models, 320/360/480dp, 100/130/200% fonts, light/dark/dynamic boundary, Simplified Chinese/Japanese/English and inherited P11 tests pass | PASS in the final replay: app 10/10, zero failure/error/skipped; P12 class 3/3 |
| P12-E005 | Formatting, architecture/privacy, source governance and Lint | `./gradlew p12Evidence --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | Spotless, Detekt, project/source boundaries, build-logic negative tests and app/accounts/settings/data Lint pass without DAO/Entity/Material/financial-write bypass | PASS in 38s: 889 tasks considered, 164 executed/725 up-to-date; strict verification, all four Lint targets, Detekt, Spotless, architecture/source policy, build-logic tests and 44 finance-domain tests pass; no production placeholder/logging or feature infrastructure import |
| P12-E006 | Minimum-SDK P12 UI compatibility | `./gradlew :app:pixel2Api28DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.app.P12UiContractDeviceTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | The exact P12 state/layout/localization matrix executes on minSdk 28 | PASS in 24s on Pixel 2 API 28: 3/3, zero failure/error/skipped |
| P12-E007 | Frozen source, lock and repository hygiene | P00—P12 validators; `python3 -m unittest discover -s scripts/tests -p 'test_*.py'`; explicit frozen-text diff; `git diff --check`; strict lock/verification checks; production placeholder/log scan | Frozen specifications unchanged, all P12 dependencies locked/verified, no visual-draft input or patch defect | PASS after final hygiene replay: all 13 stage validators and 55/55 validator tests pass; 37 lockfiles, 1,357 verification components and 2,804 SHA-256 entries are sealed; frozen textual inputs unchanged, diff check clean, production scan empty and visual drafts excluded from every command |
| P12-E008 | Immutable reference-driven financial batch completion audit | `DeterministicFinancialPlannerTest.batch context edits share one commit reverse every prior fact and are deterministic`; `ReferenceDataManagementDeviceTest.accountCardCategoryMerchantPlaceAndCheckpointRulesAreAtomicAndAudited`; inspect coordinator call graph and `DL-058` | Category historical reassignment and referenced-place split must append typed current revisions and REVERSE/APPLY facts through one coordinator-owned idempotent Room transaction | PASS: deterministic domain plan uses one canonical `BATCH_MUTATION`; API 36 SQLCipher test verifies one commit per operation, one receipt boundary, all child expected revisions, immutable source revisions/locations, exactly one reversal per prior APPLY, synchronous current category/location projections and zero partial state |

P12 is `VERIFIED`. The evidence covers the complete P12 account/card/reference-data behavior, all 23 screen contracts and both reference-driven financial batch workflows. REC-009/010 and all P13+ feature integration remain outside this promotion.

## P13 verification — category-first ordinary recording

Environment for `P13-E001`—`P13-E008`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. SQLCipher integration uses the real Android runtime and P09 Keystore-backed per-book key provider. The original four P13 goldens were generated solely from the implemented Compose tree and governed tokens; at P22, the pre-feature REC-011 asset was retained as history while its active regression moved to `P22SettlementAllocationGoldenDeviceTest` under `DL-102`. None of the excluded visual PNG/HTML drafts was opened, parsed, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P13-E001 | Exact YAML/source/test/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p13_recording.py`; inspect `P13_ORDINARY_RECORDING_MAPPING.md` | Exact 12 screens/42 states, required production ports/components/tests, three resource sets, retained original golden assets and truthful ledgers pass | PASS after final ledger update: stage `VERIFIED`, contract-only visual inputs and all target rows have P13 evidence |
| P13-E002 | Pure recording policy | `./gradlew :feature:record:testDebugUnitTest --console=plain`; `OrdinaryRecordPolicyTest` | Default priority/card compatibility, exact integer expression, validation, collapsed settlement, time/zone, attachment-import blocking and all origin return policies pass | PASS: 5/5 tests, zero failure/error/skipped |
| P13-E003 | SQLCipher/Room/coordinator end-to-end | `./gradlew :finance:data:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --console=plain`; `OrdinaryTransactionEntryDeviceTest` | Create/retry/edit/stale conflict/location execute through the coordinator; duplicate command does not duplicate facts; location/projection/book revision are atomic and integrity passes | PASS on Pixel 6 API 36: complete finance-data suite 5/5; P13 class 1/1, zero failure/error/skipped |
| P13-E004 | REC UI/state/accessibility matrix | `./gradlew :feature:record:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --console=plain`; `OrdinaryRecordUiContractDeviceTest.allFortyTwoFrozenRequiredStatesRenderInsideTheirRecDestination` | REC-001—012 exact 42 `requiredStates`, inherited REC-009 six-state/REC-010 four-state component behavior, 320/360/480dp, 100/130/200% fonts, zh-CN/ja-JP/en-US, light/dark/reduced-motion and stable accessible semantics render | PASS on Pixel 6 API 36: 6/6 tests, exact 42-state key set, zero failure/error/skipped |
| P13-E005 | Exact screenshot regression | `P13GoldenDeviceTest.categoryHomeEditorValidationAndSettlementGoldensMatchEveryPixel`; retained `feature/record/src/androidTest/assets/goldens/p13_*.png` assets | Category home, valid editor and validation compare at 360×720 with zero changed pixels; the original minimal REC-011 asset remains historical and the complete screen is verified by `P22-E005` | PASS on current API 36 regression: three P13-owned token/Compose renders compare exactly; P22 owns the expanded REC-011 pixel digest under `DL-102` |
| P13-E006 | Formatting, architecture/privacy, source governance and Lint | `./gradlew p13Check --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | Spotless, Detekt, project/source boundaries, negative build-logic tests and app/record/data Lint reject Material/DAO/Entity/direct-write/sensitive-route bypasses | PASS: all configured P13 static gates and JVM tests complete without failure |
| P13-E007 | Frozen source and repository hygiene | P00/P01/P13 validators; `python3 -m unittest discover -s scripts/tests -p 'test_*.py'`; `git diff --check`; production placeholder/log/forbidden import scan | Frozen specs unchanged, no excluded draft access, no placeholder/fake path, patch clean | PASS: baseline validators and script tests pass; frozen textual hashes remain identical and diff check is clean |
| P13-E008 | Application/root integration audit | `./gradlew :app:compileDebugKotlin :feature:record:compileDebugAndroidTestKotlin :finance:data:compileDebugAndroidTestKotlin`; inspect route/write call graph | Root fixed Save FAB, single-flight submit, SessionGate-safe routes, in-memory sensitive draft, SAF cancellation and sole financial coordinator path compile | PASS: all targets compile; route arguments are only enums/stable IDs and no feature/VM Journal/DAO construction exists |

P13 is `VERIFIED`. The evidence covers the complete P13 category-first expense/income flow and REC-001—REC-012 boundary. Specialized transactions, goal availability, settlement management, template authoring, import and later feature/release stages remain outside this promotion.

## P14 verification — transfer, opening, adjustment, and FX exchange

Environment for `P14-E001`—`P14-E008`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. SQLCipher integration uses the real Android runtime and the P09 Keystore-backed per-book key provider. Four P14 goldens were recorded solely from the implemented Compose tree and governed tokens; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P14-E001 | Exact YAML/source/privacy/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p14_multicurrency.py`; `python3 -m unittest scripts.tests.test_p14_multicurrency_contracts -v`; inspect `P14_MULTICURRENCY_MAPPING.md` | Exact five screens/15 states, typed production paths, no float/zero quote/request widening/checkpoint mutation, four goldens and truthful ledgers pass; six weakening mutations fail | PASS after final ledger update: five screens, 15 exact required states, six/six mutation tests, four contract/token-only goldens and P14 `VERIFIED` |
| P14-E002 | Exact specialized-entry and currency policy | `./gradlew :core:network:testDebugUnitTest :feature:record:testDebugUnitTest :feature:settings:testDebugUnitTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; inspect focused JUnit XML | Bounded/privacy-limited exact-decimal quote parsing, dual authoritative amounts, same-account/currency rejection, manual/cached/implied evidence, rate mismatch, spread, opening/adjustment rules and protected currency visibility/order pass | PASS: network 3/3 (P14 quote client 2/2), record 9/9 (P14 class 4/4), settings 2/2; zero failure/error/skipped |
| P14-E003 | Online/cache/manual/offline evidence policy | `FxQuoteNetworkClientTest`; `SpecializedTransactionPolicyTest`; `SpecializedTransactionEntryDeviceTest` | Request contains only source/target/date; exact decimal and bounded retry; cached quote exposes timestamp; no-cache path requires positive manual evidence; no fabricated zero or historical cache overwrite | PASS: MockWebServer proves exact request/query and retry bound; policy and API-36 SQLCipher cases pass latest, historical, cache and manual paths with distinct sources/times |
| P14-E004 | SQLCipher/Room/coordinator end-to-end | `./gradlew :finance:domain:test :finance:application:test :finance:data:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; `AccountingPlannerPropertyTest`; `SpecializedTransactionEntryDeviceTest` | Transfer/opening/adjustment/FX execute through the sole coordinator; Journals balance; transfer net assets unchanged; non-economic entries create no economic/budget effects; duplicate command is idempotent; current valuation changes only valuation revision; historical facts and immutable checkpoint remain unchanged | PASS: domain 44/44 and application 7/7 on JDK 17; full finance-data suite 6/6 on Pixel 6 API 36 including P14 class 1/1; zero failure/error/skipped; integrity/foreign-key checks and derived checkpoint association pass |
| P14-E005 | REC/SETG UI state, accessibility and boundary matrix | `./gradlew :feature:record:pixel6Api36DebugAndroidTest :feature:settings:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; focused P14 UI classes | REC-013/020/021/022 exact 13 states plus SETG-004 two states render; 320/360/480dp, 100/130/200% fonts, zh-CN/ja-JP/en-US, light/dark, scrolling summaries, fixed Save and stable semantics have no clipping/error | PASS on Pixel 6 API 36: record full suite 10/10 including P14 UI 3/3; settings 1/1; zero failure/error/skipped; exact 15-state key set covered |
| P14-E006 | Exact screenshot regression and provenance | `P14GoldenDeviceTest.specializedTransactionGoldensMatchEveryPixel`; four `feature/record/src/androidTest/assets/goldens/p14_*.png` assets | Transfer light, adjustment dark, FX light and opening dark render at 360×720 and compare with zero changed pixels; baselines originate only from contract/token Compose | PASS on API 36 as part of the 10/10 record suite: four exact pixel comparisons; no excluded visual draft was accessed |
| P14-E007 | Formatting, architecture/privacy, source governance and Lint | `./gradlew verifyArchitecture verifySourcePolicies spotlessCheck detekt :app:lintDebug :core:network:lintDebug :feature:accounts:lintDebug :feature:record:lintDebug :feature:settings:lintDebug :finance:data:lintDebug :build-logic:test --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | Frozen graph, coordinator/write/privacy rules, Detekt, Spotless, negative policy tests and every P14-relevant Lint target pass on JDK 17 without detector suppression or version drift | PASS: architecture covers 40 projects; source policy covers 121 production files/215 IDs; all listed static/JVM/Lint gates pass. The inherited Lint 32.3.1 Java-21 `List.removeLast` path was avoided with equivalent Kotlin `count`/`none` calls while retaining JDK 17 and all detectors (`DL-055`) |
| P14-E008 | Frozen source, locks, aggregate and repository hygiene | `./gradlew p14Check --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; P00/P01/P14 validators; all script tests; `git diff --check`; production placeholder/log/direct-write scans | App and Android-test integration compile; new OkHttp/serialization graph is locked and strictly verified; frozen textual specs unchanged; no excluded-draft input, placeholder, bypass or patch defect; no P15+ promotion | PASS after final replay: aggregate/static/unit/Lint gate, baseline validators and all Python mutation tests pass; dependency locks include every new transitive edge; frozen hashes remain identical and later stages remain `NOT_STARTED` |

P14 is `VERIFIED`. The evidence covers REC-013/020/021/022, SETG-004, exact historical amount/FX evidence, encrypted reference-rate caching and current valuation revision behavior. Journal detail/editing, refunds, credit/loan/settlement transaction types, analytics valuation UI and global settings/release acceptance remain in their later owning stages.

## P15 verification — journal, search/filter, immutable history, and trash

Environment for `P15-E001`—`P15-E008`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database evidence uses the real Android SQLCipher runtime and P09 Keystore-backed per-book key provider. Two P15 goldens were recorded solely from the implemented Compose tree, textual UI contract, token JSON, screen YAML and localized resources; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P15-E001 | Exact YAML/source/privacy/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p15_journal.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p15_journal_contracts -v`; inspect `P15_JOURNAL_MAPPING.md` | Exact 12 JRN screens/42 states, required production paths, no deep OFFSET/swipe/direct feature write/physical P15 purge, two goldens and truthful ledgers pass; six weakening mutations fail | PASS after final ledger update: 12 screens, 42 exact states, six/six mutation tests, two contract/token-only goldens and P15 `VERIFIED` |
| P15-E002 | Typed query/selection/bulk policy | `./gradlew :finance:domain:test :finance:data:testDebugUnitTest :feature:journal:testDebugUnitTest --offline --no-configuration-cache`; focused `TransactionSqlCompilerTest`, planner batch tests and `JournalSelectionPolicyTest` | Full same-dimension OR/cross-dimension AND filters, keyset/FTS/RTree compiler, no unsafe FTS syntax, 500,000-result selection represented only by exceptions, exact eight allowed/four forbidden bulk fields | PASS: all listed JVM suites pass; the selection retains 12 exceptions instead of 500,000 IDs and query/static mutation tests reject weakened boundaries |
| P15-E003 | Real 500,000-row SQLCipher paging/search | `./gradlew :finance:data:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.finance.data.RoomFinancialDataDeviceTest#halfMillionRowsUseBoundedKeysetPagingAndFtsWithoutDeepOffset --offline --no-configuration-cache --max-workers=1 --console=plain` | Seed exactly 500,000 current transactions; bounded page one/two do not overlap; unique FTS result is found; SQL contains no OFFSET; query plan uses the current projection index | PASS 1/1 on Pixel 6 API 36 in 3m12s; 500,000 rows and FTS rows committed under SQLCipher and both keyset pages/search assertions pass |
| P15-E004 | SQLCipher/coordinator/history/trash/filter integration | `./gradlew :finance:data:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.finance.data.JournalApplicationPortDeviceTest --offline --no-configuration-cache --max-workers=1 --console=plain` | Global balance absent/account balance present; atomic BULK_EDIT; RESTORE appends immutable history; trash nets facts and eligibility explains P31 boundary; AEAD presets survive reopen without plaintext | PASS 2/2 on Pixel 6 API 36; both transactions append BULK_EDIT, old-version restore appends RESTORE, trash is financially net-zero, physical purge remains explicitly maintenance-only, all saved-filter mutations round-trip encrypted |
| P15-E005 | JRN UI state/accessibility/responsive matrix | `./gradlew :feature:journal:pixel6Api36DebugAndroidTest --offline --no-configuration-cache --max-workers=1 --console=plain`; `JournalUiContractDeviceTest` | Every JRN-001—JRN-012 required state renders at 320/360/480dp, 100/130/200% fonts, zh-CN/ja-JP/en-US and light/dark; protected fields have no value semantics and rows remain icon/text distinguishable | PASS 2/2 on Pixel 6 API 36 in 39s; exact 42-state oracle and responsive/localized matrix pass with no missing screen root |
| P15-E006 | Exact screenshot regression and provenance | `P15GoldenDeviceTest.journalListAndDetailGoldensMatchEveryPixel`; `p15_journal_list_light.png`; `p15_journal_detail_dark.png` | List-light and complete-detail-dark render at 360×720dp and compare with zero changed pixels; only contract/token Compose output is a baseline input | PASS as part of the feature suite: two 945×1890 physical-pixel comparisons at device density; generated assets are non-empty and no excluded visual draft was accessed |
| P15-E007 | Architecture/privacy/source governance, formatting and Lint | `./gradlew p15Check --offline --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | Architecture, source-policy negative fixtures, Spotless, detekt, application/data/domain/journal tests and all P15-relevant Lint targets pass; no feature SQL/DAO/Entity/fact construction, swipe delete, sensitive route/SavedState or coordinator bypass | PASS after final replay; strict locks/verification resolve Paging 3.5.0, all root/static/JVM/Lint/build-logic gates complete without suppressing rules |
| P15-E008 | Frozen-source baseline and repository hygiene | `python3 scripts/validate_spec_baseline.py`; `python3 scripts/validate_p01_baseline.py`; `python3 -m unittest discover -s scripts/tests -p 'test_*.py'`; `git diff --check`; production placeholder/direct-write/log scans | Ten frozen files retain hashes/counts; all phase validators/mutations pass; no patch defect, visual-draft access, fake implementation or P16+ promotion | PASS after final replay: 10 frozen sources, 434 tokens, 90 requirements, 215 screens, 35 leaf projects and 37 locks remain exact; later stages remain `NOT_STARTED` |

P15 is `VERIFIED`. The evidence covers the JRN-001—JRN-012 UI/query/history/trash boundary and all P15-owned acceptance. P31 still owns the final maintenance-only physical purge transaction; P24/P34/P36 retain their explicitly documented cross-feature/final reviews.

## P16 verification — complete refund lifecycle

Environment for `P16-E001`—`P16-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database evidence uses the real Android SQLCipher runtime and P09 Keystore-backed per-book key provider. The two pixel digests are computed directly from the contract/token Compose rendering; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P16-E001 | Exact YAML/source/privacy/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p16_refunds.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p16_refund_contracts -v`; inspect `P16_REFUND_MAPPING.md` | REC-015/016 exact routes, eight required states/components, stable-ID-only argument, typed immutable refund allocation, coordinator-only writes, localization/golden/test ownership and truthful ledgers pass; six weakening mutations fail | PASS after final ledger update: two screens, eight exact states, six/six mutation tests, typed facts/ports and P16 `VERIFIED` |
| P16-E002 | Deterministic refund facts and properties | `./gradlew :finance:domain:test :feature:record:testDebugUnitTest --max-workers=1 --console=plain`; `RefundPlannerPropertyTest`; `RefundPolicyTest` | Full/multiple partial/cross-month/other account/independent/excess, inherited dimensions, exact contra-expense, APPLY/REVERSE, project/goal/budget/settlement effects, conflict validation and no floating amount path pass | PASS: finance domain 48/48 including three focused refund tests and 500 generated balanced linked refunds; record policy 13/13 including four focused refund tests; zero failure/error/skipped |
| P16-E003 | Actual-device SQLCipher/Room/coordinator lifecycle | `./gradlew :finance:data:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.finance.data.RefundApplicationPortDeviceTest --no-configuration-cache --max-workers=1 --console=plain`; inspect managed-device XML | Repeated partial/full, cross-month, other receiving account, independent and audited excess use one coordinator; dates/effects/project/goal/status/hash/idempotency/rebuild/integrity pass | PASS 2/2 on Pixel 6 API 36; SQL asserts September cash date with July accrual and budget month, refund produces no income effect, and rebuilt projection hash equals the pre-rebuild hash |
| P16-E004 | Original-transaction dependency policy | Same API 36 class; `originalTrashConvertsAllLinkedRefundsToIndependentAtomically` plus domain reversal property | Original trash cannot orphan linked refunds; explicit conversion/reverse strategies execute in one batch; trash/reversal nets Journal/economic/budget/project/goal/settlement/allocation facts to zero | PASS: conversion appends one atomic coordinator batch, preserves refund cash facts and removes every original dependency; the property test verifies exact inverse facts and zero net effect |
| P16-E005 | REC refund UI, accessibility and high-risk matrix | `./gradlew :feature:record:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.feature.record.RefundUiContractDeviceTest --no-configuration-cache --max-workers=1 --console=plain` | REC-015 linked/independent/partiallyRefunded/exceedsRemaining/saving and REC-016 content/empty/searching render; 320/360/480dp, 100/130/200% fonts, zh-CN/ja-JP/en-US, light/dark, three date dimensions and high-risk confirmation remain reachable with governed semantics | PASS 2/2 on Pixel 6 API 36; exact eight-state oracle and responsive/localized matrix pass without missing roots or clipped high-risk/date controls |
| P16-E006 | Exact screenshot digest and provenance | `P16GoldenDeviceTest.refundContractGoldensMatchPixelDigests`; linked-light and excess-dark 360×720 renders | Full physical pixels hash to the frozen two SHA-256 values and are generated only from contract/token Compose | PASS 1/1 on API 36: `47ee9bf6e33f4be287bb1e71be40c8dbec9a73073b56ba8ad9640a9eba98a6c5` and `c7c7bde39e568be56ffae791559b5fa596bbee9e1788e13b6016df244d47311e` match |
| P16-E007 | Static, Lint, aggregate baseline and repository hygiene | `./gradlew p16Check --max-workers=1 --console=plain`; focused `verifyArchitecture verifySourcePolicies spotlessCheck detekt` and app/record/data Lint; P00/P01/P16 validators; all script tests; `git diff --check`; production placeholder/direct-write/log scans | Frozen graph/specs, formatting, all detectors, financial-write/privacy rules, JVM tests and P16 Lint pass; no excluded-draft input, fake path, patch defect or P17+ promotion | PASS after final replay. The known bundled FIR/JDK-17 `List.removeLast` analyzer path was avoided by keeping refund root dispatch/title in its bounded file; JDK/toolchain and every Lint detector remain unchanged (`DL-055`) |

P16 remains `VERIFIED`. Evidence covers typed refund revisions/allocations, contra-expense and restoration effects, cumulative refundable balance, original dependency strategies, REC-015/016 and P16-owned journal/rebuild relationships. P17 now owns the completed budget screens; P25/P27 retain project/goal/analytics completion, P21 owns installment-specific allocation UI, and P31 owns maintenance-only physical purge.

## P17 verification — monthly budgets and rollover

Environment for `P17-E001`—`P17-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database evidence uses the real Android SQLCipher runtime and P09 Keystore-backed per-book key provider. Pixel digests are computed directly from the contract/token Compose rendering; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P17-E001 | Exact budget source/YAML/route/privacy/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p17_budget.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p17_budget_contracts -v`; inspect `P17_BUDGET_MAPPING.md` | BUD-001—008 exact routes/23 states, allowlisted route parameters, typed month/template/adjustment mutations, coordinator-only writes, three-language resources and truthful ledgers pass; six weakening mutations fail | PASS after final ledger update: 8 screens, 23 exact states, six/six mutation tests, typed facts/ports and P17 `VERIFIED` |
| P17-E002 | Budget hierarchy, rollover and daily-availability properties | `./gradlew :finance:domain:test :feature:planning:testDebugUnitTest --max-workers=1 --console=plain`; `BudgetRolloverPropertyTest`; `BudgetPolicyTest` | Base-only total/root/child constraints, unclassified total usage, no parent/child double count, positive/negative uncapped rollover, historical replacement, exact signed adjustment and daily formula pass without floating authority | PASS: 1,000 generated valid/invalid hierarchies, a 122-natural-month chain, historical-edit recomputation and five focused domain plus three planning-policy tests complete with zero failure/error/skipped |
| P17-E003 | Actual-device SQLCipher/Room/coordinator lifecycle and projection rebuild | `./gradlew :finance:data:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.finance.data.BudgetApplicationPortDeviceTest --no-configuration-cache --max-workers=1 --console=plain`; inspect managed-device XML | Month/template revision history, expected-revision conflict, exact duplicate idempotency, signed adjustments, account non-impact, usage/rollover versions, nonzero future reservation, integrity/FK and canonical rebuild hash pass | PASS 2/2 on Pixel 6 API 36; `(1800 - 310) / 22 = 67` remains true after a following template commit advances `localRevision`, adjustment leaves account balance unchanged, and rebuilt projection hash equals its pre-rebuild hash |
| P17-E004 | BUD UI, accessibility and responsive/localized matrix | `./gradlew :feature:planning:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.feature.planning.BudgetUiContractDeviceTest --no-configuration-cache --max-workers=1 --console=plain` | All 23 required states render; 320/360/480dp, 100/130/200% font, zh-CN/ja-JP/en-US, light/dark, wrapping actions and compact lazy scrolling keep constraint evidence/Save reachable with governed semantics | PASS 2/2 on Pixel 6 API 36; exact state oracle and compact accessibility matrix report no missing root, formula operand, meter or action |
| P17-E005 | Exact screenshot digest and provenance | `P17GoldenDeviceTest.budgetHomeAndConstraintEditorGoldensMatchEveryPixel`; configured-light and constraint-error-dark 360×720 renders | Full physical pixels hash to two frozen SHA-256 values generated only from contract/token Compose | PASS 1/1 on API 36: `837e242104cc3543dc558758396ba7c41701405e4d913d7244c4a3709f8d0684` and `1c0b85a8842cbb8ca13a0e7a527df0a80d500783b86af11cb9699b3d3638f321` match |
| P17-E006 | Static, architecture, formatting, Detekt and Lint aggregate | `./gradlew p17Check --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | Frozen dependency directions, coordinator-only financial writes, stable governed tags, no raw Material/feature SQL, formatting, Detekt, relevant JVM tests and app/planning/data Lint all pass | PASS after final replay with no gate/configuration weakening, architecture exception or feature-side DAO/entity path |
| P17-E007 | Frozen-source baseline and repository hygiene | `python3 scripts/validate_spec_baseline.py`; `python3 scripts/validate_p01_baseline.py`; `python3 -m unittest discover -s scripts/tests -p 'test_*.py'`; `git diff --check`; production placeholder/direct-write/log scans | Ten frozen files retain hashes/counts; all phase validators/mutations pass; no patch defect, excluded-draft input, fake implementation or P18+ promotion | PASS after final replay: 10 frozen sources, 434 tokens, 90 requirements, 215 screens, 35 leaf projects and 37 locks remain exact |

P17 is `VERIFIED`. Evidence covers immutable month/template history, signed adjustments, base-only hierarchy constraints, deterministic positive/negative rollover, nonzero recurrence reservation consumption, stale-projection failure semantics and all BUD-001—008 UI states. P23 retains recurrence authoring, P25/P34 retain full analytics, P28+ retain durable operation execution, and P18+ remain unpromoted.

## P18 verification — projects and goal funds

Environment for `P18-E001`—`P18-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database evidence uses the real Android SQLCipher runtime and the P09 Keystore-backed per-book key provider. Pixel digests are computed directly from contract/token Compose rendering; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P18-E001 | Exact domain/application/YAML/route/privacy/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p18_project_goal.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p18_project_goal_contracts -v`; inspect `P18_PROJECT_GOAL_MAPPING.md` | PRJ-001—006/GOL-001—005 exact routes and 31 states, allowlisted IDs/enum, typed project/goal requests, coordinator-only goal facts, bounded keyset Paging, three-language resources and truthful ledgers pass; six weakening mutations fail | PASS after final ledger update: 11 screens, 31 exact states, six/six mutation tests, typed ports/facts and P18 `VERIFIED` |
| P18-E002 | Project/goal domain and accounting properties | `./gradlew :finance:domain:test --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; `ProjectGoalPolicyTest` | 1,000 checked availability cases, SELF_SHARE/monthly snapshot, no transfer/principal usage, real loan-charge usage, archive/reopen, positive movement and stale-revision policies pass; goal movement emits no Journal/Posting | PASS: all domain tests complete with zero failure/error/skipped; target allocation changes only goal effects, negative availability remains exact, and project history retains its transaction-time snapshot |
| P18-E003 | Actual-device SQLCipher/Room/coordinator lifecycle, keyset query and rebuild | `./gradlew :finance:data:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.finance.data.ProjectGoalApplicationPortDeviceTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; inspect managed-device XML | Create/link/archive/reopen, idempotency/conflict, allocate/release/SPEND/RESTORE, monthly include/exclude, negative availability, bounded keyset pages, integrity/FK and canonical rebuild hash pass without changing actual account balance | PASS 1/1 on Pixel 6 API 36 in 21.445 s; three one-row keyset pages are unique, duplicate command writes no extra fact, stale row fails, and rebuilt project/goal hash equals the pre-rebuild hash |
| P18-E004 | PRJ/GOL UI, accessibility and responsive/localized matrix | `./gradlew :feature:planning:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.feature.planning.ProjectGoalUiContractDeviceTest,app.ledger.feature.planning.P18GoldenDeviceTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | All 31 required states render under 320/360/480dp, 100/130/200% font, zh-CN/ja-JP/en-US and light/dark boundaries; account formula, negative warning, Save, relationships and chart tables remain reachable | PASS 3/3 on Pixel 6 API 36; exact state oracle, compact 200% warning/action path and accessible project/goal chart tables pass with no missing root or clipped required action |
| P18-E005 | Exact screenshot digest and provenance | `P18GoldenDeviceTest.projectCashflowAndGoalDetailGoldensMatchEveryPixel`; project-light and underfunded-goal-dark 360×720 renders | Full physical pixels hash to two frozen SHA-256 values generated only from production Compose, tokens and textual/YAML contracts | PASS 1/1 on API 36: `ceeee3a877d527f1209a74ebef2d9465e9af3b138ba6f1e4724b444c841aee6b` and `21e60c776ed75a549424b1e41c6e3a3ca016cb09cd39514bde433a671c10f9fd` match |
| P18-E006 | Static, architecture, formatting, Detekt and Lint aggregate | `./gradlew p18Check --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | Frozen dependency directions, coordinator-only financial writes, no feature DAO/entity/SQL path, exact keyset query, governed tags/components, formatting, Detekt, JVM tests and relevant Lint pass | PASS after final replay with no gate/configuration weakening, architecture exception, feature-side financial writer or deep OFFSET path |
| P18-E007 | Frozen-source baseline and repository hygiene | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_spec_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p01_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*.py'`; `git diff --check`; production placeholder/direct-write/log scans | Ten frozen files retain hashes/counts; all phase validators/mutations pass; no patch defect, excluded-draft input, fake implementation or P19+ promotion | PASS after final replay: 10 frozen sources, 434 tokens, 90 requirements, 215 screens, 35 leaf projects and 37 locks remain exact |

P18 is `VERIFIED`. Evidence covers independent project budgets/reports and account goal reservations, immutable snapshot/fact histories, checked availability, coordinator-only financial mutation, deterministic synchronous rebuild, bounded keyset Paging and all PRJ/GOL states. P22 retains settlement management, P23 retains recurrence authoring, P25/P34 retain cross-feature analytics, and P19+ remain unpromoted.

## P19 verification — credit accounts, statements and repayment bookkeeping

Environment for `P19-E001`—`P19-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database evidence uses the real Android SQLCipher runtime and the P09 Keystore-backed per-book key provider. Pixel digests are computed directly from contract/token Compose rendering; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P19-E001 | Exact domain/application/YAML/route/privacy/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p19_credit.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p19_credit_contracts -v`; inspect `P19_CREDIT_MAPPING.md` | REC-014/CRD-001—008 exact stable-ID routes and 29 states, typed application requests, coordinator-only facts, three-language resources, two full-pixel hashes and truthful ledgers pass; six weakening mutations fail | PASS after final ledger update: 9 screens, 29 exact states, six/six mutation tests, typed ports/policies, no sensitive route argument and P19 `VERIFIED` |
| P19-E002 | Credit calendar, statement, allocation, overpayment and automatic-bookkeeping domain properties | `./gradlew :finance:domain:test`; `CreditAccountingPolicyTest` | Short-month/skipped-date/zone due rules, display-only official difference, earliest allocation, active-overpayment rejection, passive positive balance, five-fact formal eligibility, candidate no-fact path and revisioned profile/statement mutations pass | PASS: full domain suite completes with zero failure/error/skipped; all six focused credit tests pass using checked integer money and deterministic time inputs |
| P19-E003 | Actual-device SQLCipher/Room/coordinator lifecycle and projection rebuild | `./gradlew :finance:data:pixel6Api36DebugAndroidTest`; `CreditApplicationPortDeviceTest.officialEstimatedStatementsPaymentsIdempotencyAndProjectionsRemainAtomicAndRebuildable` | Profile, limits, credit consumption, estimated/official/sealed statements, assignment, repayment/allocation, conflicts, injected rollback, occurrence retry, positive balance, integrity/FK and canonical rebuild pass | PASS 15/15 on Pixel 6 API 36; the focused P19 case passes inside the full suite, duplicate command/occurrence adds no fact, active overpayment fails and rebuilt credit projection/hash equals the committed result |
| P19-E004 | REC/CRD UI, accessibility and responsive/localized matrix | `./gradlew :feature:liabilities:pixel6Api36DebugAndroidTest`; `CreditUiContractDeviceTest` | All 29 required states render under 320/360/480dp, 100/130/200% font, zh-CN/ja-JP/en-US and light/dark boundaries; overpayment, positive balance, candidate, fixed save, disclaimer and stable semantics remain reachable | PASS 3/3 on Pixel 6 API 36; exact state oracle and compact 200% high-risk matrix report no missing root, required evidence or clipped action |
| P19-E005 | Exact screenshot digest and provenance | `P19GoldenDeviceTest.creditAccountAndOfficialDifferenceGoldensMatchEveryPixel`; credit-account-light and official-difference-dark 360×720 renders | Full physical pixels hash to two frozen SHA-256 values generated only from production Compose, tokens and textual/YAML contracts | PASS 1/1 on API 36: `bac79ca0bddcf3761affcd6923cc3d2b100afcd794d58a6d233073b8c91d6299` and `cda0bb620482bd9fa90b0ba189b0ed577abab3674960ce79f43aa816f87ef433` match |
| P19-E006 | Static, architecture, formatting, Detekt, Lint and application-root regression | `./gradlew p19Check`; `./gradlew :app:pixel6Api36DebugAndroidTest` | Frozen dependency directions, coordinator-only writes, no feature DAO/entity/SQL/raw Material path, formatting, Detekt, JVM tests, relevant Lint and SessionGate/root navigation pass | PASS after final replay: P19 aggregate succeeds and app API 36 suite passes 10/10; locale-context owner propagation preserves the Activity result registry without weakening the gate or changing the frozen JDK |
| P19-E007 | Frozen-source baseline and repository hygiene | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_spec_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p01_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*.py' -v`; `git diff --check`; P19 production placeholder/direct-write/privacy scans | Ten frozen files retain hashes/counts; all static-gate mutation tests pass; no patch defect, excluded-draft input, fake implementation, direct feature financial writer or P20+ promotion exists | PASS after final replay: 91/91 script tests, 10 frozen sources, 434 tokens, 90 requirements, 215 screens, 35 leaf projects and 37 locks remain exact |

P19 is `VERIFIED`. Evidence covers complete credit profiles/statements and limits, immutable assignment/repayment/allocation facts, active-overpayment prevention, passive-positive explanation, formal/candidate auto-bookkeeping, deterministic synchronous rebuild and every REC-014/CRD state. P23 retains only occurrence-engine integration; installments, loans, analytics, widgets and P20+ remain unpromoted.

## P20 verification — credit card installments

Environment for `P20-E001`—`P20-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database evidence uses the real Android SQLCipher runtime and the P09 Keystore-backed per-book key provider. Pixel digests are computed directly from contract/token Compose rendering; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P20-E001 | Exact domain/application/YAML/route/privacy/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p20_installments.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p20_installment_contracts -v`; inspect `P20_INSTALLMENT_MAPPING.md` | REC-027/INS-001—006 exact stable-ID routes and 19 states, typed application requests, coordinator-only facts, three-language resources, two full-pixel hashes and truthful ledgers pass; six weakening mutations fail | PASS after final ledger update: 7 screens, 19 exact states, six/six mutation tests, checked-money ports/policies, no sensitive route argument and P20 `VERIFIED` |
| P20-E002 | Schedule, fee, settlement and refund domain properties | `./gradlew :finance:domain:test`; `InstallmentAccountingPolicyTest` | Generated principal conserves for extreme/tail cases; all five fee models remain distinct; remaining-principal chain reaches zero; settlement simulation is pure; refunds preserve old versions; overflow fails closed | PASS: full domain suite 69/69; focused P20 tests 5/5 including 2,000 generated schedules, checked integer money and deterministic dates/rates |
| P20-E003 | Actual-device SQLCipher/Room/coordinator lifecycle and projection rebuild | `./gradlew :finance:data:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.finance.data.InstallmentApplicationPortDeviceTest`; inspect managed-device XML | Preview/simulation write nothing; plan save, settlement and refund application are atomic/idempotent/conflict checked; purchase is not duplicated; fee is non-consumption; prior schedules, integrity/FK and canonical rebuild pass | PASS 2/2 on Pixel 6 API 36; exact retry adds no row, stale/unrelated evidence fails, every Journal balances, original consumption remains one, and rebuilt installment projection/hash equals the committed result |
| P20-E004 | REC/INS UI, accessibility and responsive/localized matrix | `./gradlew :feature:liabilities:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.feature.liabilities.InstallmentUiContractDeviceTest`; inspect managed-device XML | All 19 required states render under 320/360/480dp, 100/130/200% font, zh-CN/ja-JP/en-US and light/dark boundaries; schedule table, compact refund decision, simulation and fixed Save semantics remain reachable | PASS 2/2 on Pixel 6 API 36; exact state oracle and compact 320dp/200% dark matrix report no missing root, conservation evidence, decision or required action |
| P20-E005 | Exact screenshot digest and provenance | `P20GoldenDeviceTest.installmentDetailAndCalculatedSettlementGoldensMatchEveryPixel`; active-detail-light and calculated-settlement-dark 360×720 renders | Full physical pixels hash to two frozen SHA-256 values generated only from production Compose, tokens and textual/YAML contracts | PASS 1/1 on API 36: `c6d1de210d8502eb243278563d07b832bf5c6442b4dcfcdba51556719e2c1c53` and `1e3725075f06351a7028db744bf043c9d825febd93495ed11a431face5bd7ac9` match |
| P20-E006 | Static, architecture, formatting, Detekt, Lint and application-root regression | `./gradlew p20Check`; `./gradlew :app:pixel6Api36DebugAndroidTest` | Frozen dependency directions, coordinator-only writes, no feature DAO/entity/SQL/raw Material/floating-money path, formatting, Detekt, JVM tests, relevant Lint and SessionGate/root navigation pass | PASS after final replay: P20 aggregate completed 904 Gradle tasks and app API 36 completed 10/10 device tests without gate weakening, architecture exception or feature-side financial writer |
| P20-E007 | Frozen-source baseline and repository hygiene | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_spec_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p01_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*.py' -v`; `git diff --check`; P20 production placeholder/direct-write/privacy scans | Ten frozen files retain hashes/counts; all static-gate mutation tests pass; no patch defect, excluded-draft input, fake implementation, direct feature financial writer or P21+ promotion exists | PASS after final replay: 97/97 script tests pass; frozen source/token/requirement/screen/module/lock baselines remain exact and patch hygiene is clean |

P20 is `VERIFIED`. Evidence covers the purchase-as-one-event rule, complete versioned schedule/fee models, posted liability versus unposted commitment, pure simulation, explicit settlement/refund application, idempotent atomic SQLCipher persistence, deterministic progress rebuild and every REC-027/INS state. Loans, recurrence, analytics, widgets and P21+ remain unpromoted.

## P21 verification — loans

Environment for `P21-E001`—`P21-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database evidence uses the real Android SQLCipher runtime and the P09 Keystore-backed per-book key provider. Pixel digests are computed directly from contract/token Compose rendering; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P21-E001 | Exact domain/schema/application/YAML/route/privacy/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p21_loans.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p21_loan_contracts -v`; inspect `P21_LOAN_MAPPING.md` | 15 routes and 41 states, stable-ID-only arguments, typed application requests, coordinator-only formal writes, strict future projection, three-language resources and truthful ledgers pass; six weakening mutations fail | PASS after final ledger update: exact screen/state oracle, checked-money ports/policies, no sensitive route argument, six/six mutation rejection and P21 `VERIFIED` |
| P21-E002 | Schedule, rate, payment and prepayment domain properties | `./gradlew :finance:domain:test`; `LoanAccountingPolicyTest` | Five repayment methods conserve principal; rate stages do not overlap or leave scheduled gaps; payment components reconcile and cap principal; partial/full prepayment and overflow fail closed | PASS: full domain suite 75/75; focused P21 tests 6/6 including 2,000 generated schedules, all payment frequencies/methods, exact tails and deterministic checked arithmetic |
| P21-E003 | Actual-device SQLCipher/Room/coordinator lifecycle and projection rebuild | `./gradlew :finance:data:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.finance.data.LoanApplicationPortDeviceTest`; inspect managed-device XML | Two-tranche contract, future-only plan, disbursement/payment, exact replay, conflict, pure simulation, confirmed apply, version retention, integrity/FK and canonical rebuild pass | PASS 1/1 on Pixel 6 API 36: every Journal balances, component/principal invariants hold, exact retries add no facts, simulation does not change book revision, apply appends versions and rebuilt projection/hash matches |
| P21-E004 | LIA/REC/LOA UI, accessibility and responsive/localized matrix | `./gradlew :feature:liabilities:pixel6Api36DebugAndroidTest`; `LoanUiContractDeviceTest` | All 41 required states render under 320/360/480dp, 100/130/200% font, zh-CN/ja-JP/en-US and light/dark boundaries; combination totals, planned/actual, reconciliation and fixed actions remain reachable | PASS: focused matrix 2/2 and full liabilities regression 10/10 on Pixel 6 API 36; compact 320dp/200% high-risk combinations report no missing root, conservation evidence or required action |
| P21-E005 | Exact screenshot digest and provenance | `P21GoldenDeviceTest.loanCombinationDetailAndPrepaymentResultGoldensMatchEveryPixel`; multi-tranche-detail-light and simulation-result-dark 360×720 renders | Full physical pixels hash to two frozen SHA-256 values generated only from production Compose, tokens and textual/YAML/CSV contracts | PASS 1/1 on API 36: `6f00bc134059ef0de333706f3aacfffdc36d16a1138f8f4f3f5a9627228421d4` and `8d3b4240181052ed95a2a9142df4639c0a109561eecd32f184cfbb682464d9cd` match |
| P21-E006 | Static, architecture, formatting, Detekt, Lint and application-root regression | `./gradlew p21Check`; `./gradlew :app:pixel6Api36DebugAndroidTest` | Frozen dependency directions, coordinator-only writes, no feature DAO/entity/SQL/raw Material/floating-money path, formatting, Detekt, JVM tests, relevant Lint and SessionGate/root navigation pass | PASS after final replay: P21 aggregate succeeds and app API 36 suite passes 9/9 without gate weakening, architecture exception or feature-side financial writer |
| P21-E007 | Frozen-source baseline and repository hygiene | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_spec_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p01_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*.py' -v`; `git diff --check`; P21 production placeholder/direct-write/privacy scans | Ten frozen files retain hashes/counts; all static-gate mutation tests pass; no patch defect, excluded-draft input, fake implementation, direct feature financial writer or P22+ promotion exists | PASS after final replay: 103/103 script tests pass; frozen source/token/requirement/screen/module/lock baselines remain exact and patch hygiene is clean |

P21 is `VERIFIED`. Evidence covers combination contracts and tranche ledgers, immutable terms/rates/schedules, all repayment methods, exact actual allocations, pure and confirmed prepayment simulation, idempotent atomic SQLCipher persistence, deterministic progress/future-only rebuild and every LIA/REC/LOA state. At the P21 handoff, P22 and later work remained unpromoted.

## P22 verification — mutual-settlement netting

Environment for `P22-E001`—`P22-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database evidence uses the real Android SQLCipher runtime and the P09 Keystore-backed per-book key provider. Pixel digests are computed directly from production Compose rendering and frozen textual/YAML/CSV/token inputs; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P22-E001 | Exact domain/schema/application/YAML/route/privacy/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p22_settlements.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p22_settlement_contracts -v`; inspect `P22_SETTLEMENT_MAPPING.md` | Exact 9 routes/27 states, StableId-only arguments, closed checked allocation, normalized settlement schema, coordinator ownership, three-language resources and truthful ledgers pass; seven weakening mutations fail | PASS after final ledger update: exact route/state oracle, all required production/evidence tokens, resource parity and 7/7 mutation rejection pass |
| P22-E002 | Allocation, suggestions and self-share accounting properties | `./gradlew :finance:domain:test :finance:data:testDebugUnitTest :feature:record:testDebugUnitTest`; `SettlementAllocationPolicyTest`; `OrdinaryRecordPolicyTest`; settlement planner cases | Equal/fixed/percentage/weight, exclusion, tax/service fee, remainder, self/external payer, overflow, Journal/effect classification and suggestions conserve exact amounts | PASS: finance domain 79/79, finance data JVM 3/3 and record policy suite 15/15; focused P22 tests include 2,000 generated allocations, four closed modes and exact self-only consumption |
| P22-E003 | Actual-device SQLCipher/Room/coordinator lifecycle and projection rebuild | `./gradlew :finance:data:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.finance.data.SettlementApplicationPortDeviceTest`; inspect managed-device XML | Unique self, arbitrary participants, self/external payer, external-to-external, partial settlement, retry/conflict/failure atomicity, post-settlement edit, integrity/FK/Journal balance and canonical rebuild pass | PASS 2/2 on Pixel 6 API 36: external-only paths write no local financial fact; replay adds no row; edit retains payment digest; every Journal balances and rebuilt position/hash equals committed state |
| P22-E004 | REC/SET UI, accessibility and responsive/localized matrix | `./gradlew :feature:settlement:pixel6Api36DebugAndroidTest`; `SettlementUiContractDeviceTest`; `./gradlew :feature:record:pixel6Api36DebugAndroidTest` | All 27 P22 required states plus REC regressions render under 320/360/480dp, 100/130/200% font, zh-CN/ja-JP/en-US and light/dark boundaries; external account conditional and supplemental-history notices remain reachable | PASS: settlement 3/3 and full record regression 14/14 on Pixel 6 API 36; exact SET state oracle, high-risk 320dp/200% cases and all prior REC suites have zero failures/skips |
| P22-E005 | Exact screenshot digests and provenance | `P22SettlementAllocationGoldenDeviceTest.completeAllocationGoldenMatchesEveryPixel`; `P22GoldenDeviceTest.detailAndExternalPaymentGoldensMatchEveryPixel` | Full physical pixels of REC-011 complete allocation, SET-004 activity detail and SET-006 external-only settlement match governed production renders | PASS 2 classes / 3 renders on API 36: `10ac3fb165b6f3881f6c0e1c3b03f8aa8d7ce3bbe6e50f16b1f58760287767b4`, `f9d6c0b5ed020985df1555241ceadb1453d21ff6471335346ac502a9f1515c42` and `1e3acfa652268c86f0015504ffe374b468f6469730d6329d86ebbdbe6f805a19` match |
| P22-E006 | Static, architecture, formatting, Detekt, Lint and application-root regression | `./gradlew p22Check`; `./gradlew :app:pixel6Api36DebugAndroidTest` | No feature DAO/entity/SQL/raw Material/floating-money/direct financial writer; formatting, Detekt, relevant Lint, JVM tests, SessionGate and root navigation pass | PASS after final replay: P22 aggregate succeeds and app API 36 suite passes 10/10 without boundary weakening, sensitive route data or bypass path |
| P22-E007 | Frozen-source baseline and repository hygiene | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_spec_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p01_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*.py' -v`; `git diff --check`; production placeholder/privacy scans | Ten frozen sources retain hashes/counts; all static mutation tests pass; no patch defect, excluded-draft input, fake implementation or P23+ promotion exists | PASS after final replay: 110/110 script tests pass; 434 token scalars, 90 requirements, 215 screens, 646 states, 35 invariants, module/lock baselines and patch hygiene remain exact |

P22 is `VERIFIED`. Evidence covers arbitrary participants, all closed allocation/charge/rounding paths, self-conditional accounting, partial and pure-external settlements, immutable historical payments, deterministic positions/supplemental suggestions and every REC-011/SET state. P23 retains recurrence/occurrence integration, P25/P34 retain cross-feature analytics, and no later stage is promoted.

## P23 verification — quick templates, recurrence and candidates

Environment for `P23-E001`—`P23-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database evidence uses the real Android SQLCipher runtime and the P09 Keystore-backed per-book key provider. Pixel digests are computed directly from production Compose rendering and frozen textual/YAML/CSV/token inputs; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P23-E001 | Exact recurrence/application/schema/YAML/route/privacy/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p23_automation.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p23_automation_contracts -v`; inspect `P23_AUTOMATION_MAPPING.md` | REC-026/AUT-001—010 exact stable-ID routes and 25 states, normalized immutable schema, candidate fact isolation, operationId-only Worker input, restricted headless lease, three-language resources and truthful ledgers pass; nine weakening mutations fail | PASS after final ledger update: 11 screens, 25 exact states, 9/9 mutation rejection, no sensitive route/Worker argument or lease bypass and P23 `VERIFIED` |
| P23-E002 | Deterministic recurrence and application policy properties | `./gradlew :finance:domain:test :finance:data:testDebugUnitTest :feature:automation:testDebugUnitTest :app:testDebugUnitTest :core:navigation:testDebugUnitTest`; `RecurrenceEngineTest`; `AutomationPolicyTest`; `RecurrenceCatchUpWorkerContractTest` | Daily/business-day/weekly/monthly/nth-weekday/month-interval/year/custom rules, missing-day/weekend policies, explicit zones, exceptions, ten-occurrence preview, retry names and candidate isolation are deterministic and checked | PASS: finance domain 82/82, finance data 3/3, automation 3/3, app 4/4 and navigation 9/9; recurrence includes 2,000 generated schedules with no floating-point authority path |
| P23-E003 | Actual-device SQLCipher/Room/coordinator/lease lifecycle, replay and candidate confirmation | `./gradlew :finance:data:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.finance.data.AutomationApplicationPortDeviceTest`; `./gradlew :app:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.app.HeadlessRecurrenceExecutorDeviceTest`; inspect managed-device XML | Startup/repeated catch-up/restart/manual retry is idempotent; candidate confirmation commits normal facts and acceptance atomically through the coordinator; locked-process execution uses and releases the restricted lease; all modification scopes append audit history; integrity/FK/Journal balance pass | PASS 3/3 data plus 1/1 headless app test on Pixel 6 API 36: real Keystore/SQLCipher startup accepts `RECURRENCE_WRITE`, exact occurrence key prevents duplicate rows/facts, candidate-only catch-up changes no book revision or financial projection, confirmation consumes the candidate in the same financial transaction and every Journal balances |
| P23-E004 | REC/AUT UI, accessibility and responsive/localized matrix | `./gradlew :feature:automation:pixel6Api36DebugAndroidTest`; `AutomationUiContractDeviceTest`; `./gradlew :feature:record:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.feature.record.OrdinaryRecordUiContractDeviceTest#rec026ContentAndEmptyRenderInRecordModuleAcrossAccessibilityBoundary` | All 25 required states render in the frozen owner module under 320/360/480dp, 100/130/200% font, zh-CN/ja-JP/en-US and light/dark boundaries; candidate, invalid, paused, empty and scope paths remain reachable and accessible | PASS: automation 2/2 and focused REC-026 1/1 on Pixel 6 API 36; exact state oracle and compact 320dp/200% cases report no missing root, required evidence, clipped action or false real-payment claim |
| P23-E005 | Exact screenshot digests and provenance | `AutomationUiContractDeviceTest.p23ProductionGoldensMatchEveryPixel`; automation hub light and candidate list dark 360×720 renders | Full physical pixels hash to two frozen SHA-256 values generated only from production Compose, governed tokens and textual/YAML contracts | PASS 1/1 on API 36: `0f73c7a9ae623b1482cb53b37a0a1851ac4689868ada7a8cce81e047a8d6f874` and `7b9f1701eccafa0c06de9f2bccee96be12e9a0fcb3ba330dbd0276907e77e497` match |
| P23-E006 | Static, architecture, formatting, Detekt, Lint and application-root regression | `./gradlew p23Check`; `./gradlew :app:pixel6Api36DebugAndroidTest` | Frozen dependency directions, coordinator-only financial writes, no feature DAO/entity/SQL/raw Material/floating-money path, formatting, Detekt, relevant Lint, WorkManager/lease contract, SessionGate and root navigation pass | PASS after final replay: P23 aggregate completes 920 Gradle tasks and app API 36 suite passes 11/11 without gate weakening, lease bypass, sensitive work/route data, duplicate formal record or feature-side financial writer |
| P23-E007 | Frozen-source baseline and repository hygiene | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_spec_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p01_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*.py' -v`; `git diff --check`; production placeholder/direct-write/privacy scans | Ten frozen sources retain hashes/counts; all static mutation tests pass; no patch defect, excluded-draft input, fake implementation, direct feature financial writer or P24+ promotion exists | PASS after final replay: 119/119 discovered script tests pass; 434 token scalars, 90 requirements, 215 screens, 646 states, 35 invariants, 37 lockfiles, 1,366 verification components, 2,819 hashes and patch hygiene remain exact |

P23 is `VERIFIED`. Evidence covers immutable quick-template/series revisions, deterministic occurrence generation and edit scopes, exact-once catch-up, fact-free candidates, atomic normal-form confirmation, truthful credit/loan/ordinary integration and every REC-026/AUT state. P24 and later stages remain unpromoted.

## P24 verification — batch entry and bulk edit

Environment for `P24-E001`—`P24-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database evidence uses the actual Android SQLCipher runtime and P09 Keystore-backed per-book key provider. Pixel digests are computed from production Compose output and frozen textual/YAML/CSV/token inputs; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P24-E001 | Exact YAML/application/route/draft/coordinator/bulk-field/static boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p24_batch.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p24_batch_contracts -v`; inspect `P24_BATCH_MAPPING.md` | Five routes, 18 exact states, complete in-memory rows, StableId-only route, virtualized model, coordinator-only writes, three-language resources and truthful ledgers pass; eight weakening mutations fail | PASS after final ledger update: exact contract/source/evidence tokens pass and 8/8 coordinator, SQL, SavedState, route, eager-list, forbidden-field and swipe-delete mutations are rejected |
| P24-E002 | Deterministic batch planner, bounded query selection and allowed-field policies | `./gradlew :finance:domain:test :finance:data:testDebugUnitTest :feature:record:testDebugUnitTest :feature:journal:testDebugUnitTest :core:navigation:testDebugUnitTest`; focused batch planner and `JournalSelectionPolicyTest` | Child plans combine deterministically under one balanced parent commit; query selection stores fingerprint/exclusions; prohibited edit fields remain unrepresentable | PASS: finance domain 83/83, finance data 3/3, record 15/15, journal 2/2 and navigation 9/9; zero failure/error/skipped |
| P24-E003 | Actual-device SQLCipher/Keystore atomicity, retry, projection, audit and reversal | `./gradlew :finance:data:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.finance.data.BatchEntryApplicationPortDeviceTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | Invalid row writes nothing; failure after immutable facts rolls back; two rows use one commit/receipt/revision; exact retry is idempotent; audit and whole-batch reversal preserve history; integrity/projection versions pass | PASS 1/1 on Pixel 6 API 36: injected failure leaves genesis counts, retry remains two transactions/one receipt, every projection is revision-aligned, undo appends two reverse revisions and account net returns to zero |
| P24-E004 | REC/JRN state, accessibility, responsive/localized and scale matrix | `./gradlew :feature:record:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.feature.record.BatchRecordUiContractDeviceTest`; `./gradlew :feature:journal:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.feature.journal.JournalUiContractDeviceTest` | All ten REC-023—025 and eight JRN-005/006 states render at the frozen width/font/locale/theme boundaries; a 100,000-row summary composes fewer than 100 row/action semantics | PASS: record 3/3 and journal 1/1 on Pixel 6 API 36; exact state oracle, zh-CN/ja-JP/en-US, 320/360/480dp, 100/130/200%, light/dark and bounded LazyColumn assertions pass |
| P24-E005 | Exact production pixel digests and provenance | `BatchRecordUiContractDeviceTest.tokenAndYamlDerivedPixelGoldensRemainStable`; REC-023 light, REC-024 dark and REC-025 light 360×720 renders | Every ARGB pixel matches baselines created only from production Compose, governed tokens and textual/YAML contracts | PASS on API 36: `a207b0736bfcd848e9ab6f22d64bff60a78a6c4ca2199d0f6c2d8d459e61e044`, `8ed41793fa25efcf05748f91db243563438f3b1e73139d8102429364bf0c6745` and `a2b7abdb5d31b650f3ff52d3352be6a7e2476c2c6ca2128ff54fed58cfc61c96` match after an independent replay |
| P24-E006 | Formatting, Detekt, Lint, architecture and application-root regression | `./gradlew p24Check`; `./gradlew :app:pixel6Api36DebugAndroidTest` | No feature DAO/entity/SQL/raw Material/floating-money/direct writer; formatting, Detekt, relevant Lint, SessionGate and root navigation pass | PASS after final replay: P24 aggregate completes 926 Gradle tasks and the full API 36 application suite passes 11/11 without boundary weakening, sensitive route state or second financial writer |
| P24-E007 | Frozen baseline, complete script regression and repository hygiene | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_spec_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p01_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*.py' -v`; `git diff --check`; production placeholder/direct-write/privacy scans | Frozen hashes/counts remain exact; all static mutation tests pass; no patch defect, prohibited draft input, placeholder, fake persistence or P25+ promotion exists | PASS after final replay: 127/127 script tests; 434 token scalars, 90 requirements, 215 screens, 646 states, 35 invariants, 37 lockfiles, 1,366 verification components and 2,819 hashes remain exact; repository scans are clean |

P24 is `VERIFIED`. Evidence covers complete-row batch editing, default all-or-nothing validation and commit, failure rollback, exact retry, immutable audit/reversal, bounded query selection, closed bulk fields and every REC-023—025/JRN-005—006 state. P25 and later stages remain unpromoted.

## P25 verification — analytics engine, fixed reports and data integrity

Environment for `P25-E001`—`P25-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database tests use the actual Android SQLCipher runtime and P09 Keystore-backed per-book key provider. Pixel digests are computed from production Compose output and frozen textual/YAML/CSV/token inputs; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P25-E001 | Exact AST/catalog/YAML/route/compiler/static boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p25_analytics.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p25_analytics_contracts -v`; inspect `P25_ANALYTICS_MAPPING.md` | Exact six routes/20 states, 20 reports, bounded closed AST, bound SQL, StableId/opaque-key-only routes, coordinator-owned projection integration, three-language resources and truthful ledgers pass; eight weakening mutations fail | PASS after final ledger replay: exact catalog/state/source/evidence tokens pass and 8/8 catalog, bound, OFFSET, plaintext, integrity, accessibility and route mutations are rejected |
| P25-E002 | Fixed-report accounting semantics, visualization policy and compiler plans | `./gradlew :analytics:domain:test :analytics:data:testDebugUnitTest :feature:analysis:testDebugUnitTest`; `P25FixedReportContractTest`; `ReportSqlCompilerTest`; `AnalysisPolicyTest` | Every fixed report compiles from a closed source; values are bound; savings rate/refund/transfer/credit/loan/FX/currency and pie-fallback policies are exact; no arbitrary SQL, formula, floating authoritative amount or deep OFFSET exists | PASS: all focused domain/data/feature unit tests pass with zero failure/error/skipped; all 20 definitions compile and malicious keys fail closed |
| P25-E003 | Actual-device SQLCipher report execution, revision gating, integrity and rebuild | `./gradlew :analytics:data:pixel6Api36DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.analytics.data.AnalyticsSqlCipherDeviceTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | Correct-key encrypted open; all 20 generated queries execute against real Schema v1; immutable financial semantics, native/base currency evidence, stale rejection, nine checks and savepoint rebuild/canonical-hash equality pass | PASS 2/2 on Pixel 6 API 36: every fixed query executes with bound arguments; stale rollup is not presented, repair restores the target revision and identical canonical hash, and passphrase memory is cleared |
| P25-E004 | ANA state, accessibility, responsive/font/locale/theme matrix | `./gradlew :feature:analysis:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; `AnalysisUiContractDeviceTest` | All 20 required states render at 320/360/480dp, 100/130/200% font, zh-CN/ja-JP/en-US and light/dark boundaries; chart summaries, exact data tables, error/stale/expired paths and actions remain reachable | PASS 3/3 on Pixel 6 API 36: exact state oracle and high-risk accessibility combinations report no missing root, clipped action or absent text alternative |
| P25-E005 | Production pixel digests and forbidden-draft provenance | `P25GoldenDeviceTest.reportAndIntegrityGoldensMatchEveryPixel`; ANA-001 content light and ANA-015 failed dark at 360×720 | Every ARGB pixel matches a baseline generated only from production Compose, governed tokens and textual/YAML contracts | PASS on API 36: `3d4c0905ebeb7ae110835a2505f76a6017f549d2b440a64ecc63e373956e53c0` and `05837adc39a458f3561155595422b88d95a4d76baa509454627740ae4031103b` match |
| P25-E006 | Formatting, Detekt, Lint, architecture and application-root regression | `./gradlew p25Check --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; `./gradlew :app:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | No feature DAO/entity/SQL/raw Material/direct financial writer; formatting, Detekt, relevant Lint, complete JVM tests, SessionGate and typed analysis navigation pass | PASS after final replay: P25 aggregate completes 920 Gradle tasks and the full API 36 application suite passes 11/11 without boundary weakening, sensitive route state, unwrapped chart/material access or second financial writer |
| P25-E007 | Frozen source, full script regression and repository hygiene | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_spec_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p01_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*.py' -v`; `git diff --check`; production placeholder/direct-write/privacy scans | Frozen hashes/counts remain exact; all static mutation tests pass; no patch defect, prohibited draft input, placeholder, fake persistence or P26+ promotion exists | PASS after final replay: 135/135 script tests; 434 token scalars, 90 requirements, 215 screens, 646 states, 35 invariants, 37 lockfiles, 1,366 verification components and 2,819 hashes remain exact; repository scans are clean |

P25 is `VERIFIED`. Evidence covers the bounded query language, all 20 fixed reports, accounting/valuation/currency semantics, twelve deterministic projections, nine integrity checks, keyset drilldown/export interfaces, accessible Vico/table presentation and all ANA-001—005/015 states. P26 and later stages remain unpromoted.

## P26 verification — custom reports, dashboards, anomaly and forecast

Environment for `P26-E001`—`P26-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database tests use the actual Android SQLCipher runtime. Pixel digests are computed from production Compose output and frozen textual/YAML/CSV/token inputs; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P26-E001 | Exact YAML/state/schema/route/export/AI-static boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p26_custom_analytics.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p26_custom_analytics_contracts -v`; inspect `P26_CUSTOM_ANALYTICS_MAPPING.md` | Exact seven routes/16 states, normalized revision storage, StableId/closed-key routes, typed P29 export handoff, three languages and truthful ledgers pass; seven weakening mutations fail | PASS after final ledger replay: exact state/source/evidence tokens pass and 7/7 deterministic-authority, encryption, revision, route, accessibility and Schema-v2 security-startup drift mutations are rejected |
| P26-E002 | Deterministic anomaly/forecast/derived-series and visualization policy | `./gradlew :analytics:domain:test :feature:analysis:testDebugUnitTest`; `P26DeterministicAnalyticsContractTest`; `AnalysisPolicyTest` | Same input/order-independent series/date/version yields the same finding/result; exact recurrence, missing historical years, moving average/trend/forecast and chart fallback behavior pass without Float/Double authority | PASS: focused P26 deterministic suite passes 5/5 plus complete analytics-domain and feature-analysis unit suites with zero failure/error/skipped |
| P26-E003 | Actual-device SQLCipher normalized persistence, migration and deterministic query integration | `./gradlew :analytics:data:pixel6Api36DebugAndroidTest`; `./gradlew :core:database:pixel6Api36DebugAndroidTest`; `AnalyticsSqlCipherDeviceTest`; `EncryptedSchemaV1DeviceTest` | Report save/edit/copy conflict, multiple dashboard order/width, anomaly revision/finding, forecast/reopen, encrypted Schema v2 create and real v1→v2 data retention/integrity pass | PASS on Pixel 6 API 36: analytics data 3/3 and database 5/5; retained v1 book row, v2 contract hash, normalized rows, wrong revision rejection, correct-key reopen and integrity checks all pass |
| P26-E004 | ANA state, accessibility, responsive/font/locale/theme matrix | `./gradlew :feature:analysis:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; `AnalysisUiContractDeviceTest` | All P25+P26 36 required states render across 320/360/480dp, 100/130/200% font, zh-CN/ja-JP/en-US and light/dark boundaries; summaries/data tables/actions and missing/invalid paths remain reachable | PASS 4/4 on a clean Pixel 6 API 36 Managed Device after removing a stale offline test AVD; zero skipped/failed and all 16 P26 states satisfy the exact oracle |
| P26-E005 | Production pixel digests and forbidden-draft provenance | `P26GoldenDeviceTest.builderAndForecastGoldensMatchEveryPixel`; ANA-008 editing light and ANA-014 content dark at 360×720 | Every ARGB pixel matches a baseline produced only from production Compose, governed tokens and textual/YAML contracts | PASS on API 36: `8fdc298365d26cddbfa1f6f92604f83b2fe1ebdf3d36daff2fbb05a8953aeee5` and `62f5065b1c0cc5d6013bc609a738bbd6f9745413131ab0c63d9adf8d372cf25d` match |
| P26-E006 | Formatting, Detekt, Lint, architecture and application-root regression | `./gradlew p26Check --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; `./gradlew :app:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | No feature DAO/entity/SQL/raw Material/direct writer; no AI/OCR/formula/script surface; formatting, Detekt, relevant Lint, SessionGate and opaque P26 navigation pass | PASS after final replay: 924-task aggregate succeeds and the full API 36 application suite passes 11/11. The replay found and repaired a stale security-startup Schema-v1 literal; headless lease and cold-start Ready scenarios then pass both focused 2/2 and full-suite execution |
| P26-E007 | Frozen source, full script regression and repository hygiene | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_spec_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p01_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*.py' -v`; `git diff --check`; production placeholder/direct-write/privacy scans | Frozen hashes/counts remain exact; every static mutation passes; no patch defect, prohibited draft input, placeholder, fake persistence or P27+ promotion exists | PASS after final replay: all 142 script tests pass; 434 token scalars, 90 requirements, 215 screens, 646 states, 35 invariants, locks/verification metadata and patch hygiene remain exact |

P26 is `VERIFIED`. Evidence covers revisioned custom report/copy/dashboard/rule storage, deterministic anomaly/forecast/derived series, actual SQLCipher Schema v2 migration, safe routes, typed P29 export handoff, accessible Vico/table UI and all ANA-006—010/013/014 states. P27 and later stages remain unpromoted.

## P27 verification — consumption map

Environment for `P27-E001`—`P27-E007`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 6 API 36 Google APIs x86_64 Gradle Managed Device under KVM. Database tests use the actual Android SQLCipher runtime; map tests instantiate the actual MapLibre Android view/lifecycle. Pixel digests are computed from production Compose output and frozen textual/YAML/CSV/token inputs; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P27-E001 | Exact YAML/route/query/SDK/privacy/resource/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p27_consumption_map.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p27_consumption_map_contracts -v`; inspect `P27_CONSUMPTION_MAP_MAPPING.md` | Exact two routes/9 states, typed modes/filters, bounded RTree data path, StableId-only detail, MapLibre ownership, three-language parity and truthful ledgers pass; eight weakening mutations fail | PASS after final ledger replay: exact ANA-011/012 routes and states pass; 8/8 RTree/node-bound/exclusion/teal/table/route/geocoder/same-dimension-OR mutations are rejected |
| P27-E002 | Pure query, viewport, filter-selection and accounting policy | `./gradlew :analytics:domain:test :core:geo:test`; `./gradlew :app:testDebugUnitTest --tests app.ledger.app.ConsumptionMapFilterRemovalTest`; `ConsumptionMapContractTest`; `LedgerMapContractTest` | Current-month/default exclusions, four modes, amount/count, merchant/place, exact bounded E7/dateline viewport, same-dimension OR and independent removal, filter limits, render ceiling and SDK viewport contract are closed and deterministic | PASS: complete analytics-domain/core-geo JVM suites and the focused app filter test pass with zero failure/error/skipped; temporarily unavailable option data preserves active filters, and no floating authoritative amount/coordinate or online geocoder path exists |
| P27-E003 | Actual-device SQLCipher/RTree/history/scale/detail | `./gradlew :analytics:data:pixel6Api36DebugAndroidTest`; `AnalyticsSqlCipherDeviceTest` | Frozen base values, default/special modes, complete bounded option queries, opaque revision-aware drilldown, exact viewport and database aggregation over 10,000 located transactions pass without raw full-list/`OFFSET` loading | PASS 5/5 on Pixel 6 API 36: default consumption is JPY 3,000/2, explicit all-located is JPY 17,000/4, two active accounts are returned as options, 10,004 total located rows yield exactly 512 map nodes, and a narrow viewport remains bounded |
| P27-E004 | Actual MapLibre lifecycle plus ANA responsive/accessibility matrix | `./gradlew :core:geo:pixel6Api36DebugAndroidTest`; `./gradlew :feature:analysis:pixel6Api36DebugAndroidTest`; `LedgerMapAndPermissionDeviceTest`; `AnalysisUiContractDeviceTest` | Real cluster/heat/single layers, incremental viewport, attribution, distinct user location, failure list, independently removable same-dimension OR chips, exact nine states, 320/360/480dp, 100/130/200%, zh-CN/ja-JP/en-US, light/dark and privacy semantics pass | PASS: core geo 5/5 and feature analysis 7/7 on sequential Pixel 6 API 36 runs; 45-case P25+P26+P27 state matrix and two-account chip interaction have zero missing state, clipped action, inaccessible data alternative or coordinate semantic leak |
| P27-E005 | Production pixel digests and forbidden-draft provenance | `P27GoldenDeviceTest.consumptionMapAndDetailGoldensMatchEveryPixel`; ANA-011 clusters light and ANA-012 place dark at 360×720 | Every ARGB pixel matches baselines created only from production Compose, governed tokens and textual/YAML contracts | PASS on API 36: `4bfbfc87834852c480b51e4fd411ce3f26b4d04217c1a40ae50f459bf181304d` and `bcf9ce902212d1aa707f3e5a5b5f8e901c63e9255deddf0fe4f6740792e3cc61` match |
| P27-E006 | Formatting, Detekt, Lint, architecture, source policy and aggregate regression | `./gradlew :app:testDebugUnitTest --tests app.ledger.app.ConsumptionMapFilterRemovalTest p27Check --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; `./gradlew :app:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | No feature SQL/DAO/SDK/raw Material/direct financial writer; no sensitive route, unbounded map node list, online place lookup or red/green heat; all focused compile/unit/Lint gates and app-root integration pass | PASS after final replay: the 935-task combined P27 aggregate succeeds and the full API 36 application suite passes 11/11; formatting, Detekt, Lint, architecture/source policy, SessionGate and app-owned MapLibre host remain clean |
| P27-E007 | Frozen baseline, complete script regression and repository hygiene | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_spec_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p01_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*.py' -v`; `git diff --check`; production placeholder/direct-write/privacy scans | Frozen hashes/counts remain exact; every static mutation passes; no patch defect, excluded-draft input, placeholder, fake persistence or P28+ promotion exists | PASS after final replay: 150/150 script tests pass; 434 token scalars, 90 requirements, 215 screens, 646 states, 35 invariants and repository patch hygiene remain exact |

P27 is `VERIFIED`. Evidence covers current-month/four-mode geographic analytics, complete typed filters, immutable historical FX/accounting meanings, RTree plus exact viewport filtering, 10,000-row bounded aggregation, governed MapLibre cluster/heat/single rendering, attribution, equivalent accessible failure data and every ANA-011/012 state. P28 and later stages remain unpromoted.

## P28 verification — CSV/XLSX general and structured import

Environment for `P28-E001`—`P28-E008`: Fedora host; Temurin JDK 17.0.20; Gradle 9.5.1; AGP 9.3.1/Lint 32.3.1; Kotlin 2.4.10; compile/target SDK 36; Pixel 2 API 28 and Pixel 6 API 34/API 36 Google APIs Gradle Managed Devices under KVM. Database evidence uses actual Android SQLCipher and the P09 Keystore-backed book-key provider. Streaming JVM tests run with a hard 256 MiB maximum heap. Pixel digests are computed only from production Compose and frozen textual/YAML/CSV/token inputs; none of the excluded visual PNG/HTML drafts was opened, parsed, sampled, measured, compared or used.

| Evidence ID | Scope | Command/test | Acceptance | Result |
|---|---|---|---|---|
| P28-E001 | Exact parser/dependency/workflow/YAML/privacy/ledger boundary | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p28_import.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_p28_import_contracts -v`; inspect `P28_IMPORT_MAPPING.md` | Commons CSV 1.14.1, ICU4J 78.3, FastExcel 0.20.2 and official StAX adapter are exact; Apache POI is absent; nine stages, 15 structured kinds, ten routes/34 states, operation-ID-only Worker/route payloads, encrypted staging and coordinator-only writes pass; ten weakening mutations fail | PASS after final ledger replay: exact counts/dependencies/sources/evidence pass and all 10/10 weakening mutations are rejected |
| P28-E002 | Streaming, preparation and durable recovery on bounded JVM heap | `./gradlew :transfer:domain:test :transfer:data:testDebugUnitTest :finance:application:test --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; `ImportStreamReaderTest`; `ImportPreparationServiceTest`; `ImportIngestionRecoveryTest` | Stream 100,000 non-ASCII CSV/XLSX rows; BOM/ICU, multi-sheet, exact date/numeric/shared-string/cached-formula values, corrupt/legacy/cancel, all structured kinds, split rejection, duplicate choice, missing entities and 256-row crash/pause/cancel checkpoints pass without whole-file materialization | PASS: transfer-data 12/12 focused tests under `maxHeapSize=256m`; parser peak buffered row count is one; retry resumes after the durable boundary without duplicate staging rows and safe cancel destroys staging |
| P28-E003 | Android-runtime CSV/ICU/FastExcel compatibility | `./gradlew :transfer:data:pixel2Api28DebugAndroidTest`; focused `ImportParserCompatibilityDeviceTest` on `pixel6Api34` and `pixel6Api36` | The exact Commons CSV/ICU4J/FastExcel/StAX graph parses CSV and XLSX on min/representative/target Android runtimes without a POI fallback | PASS 1/1 on API 28, 1/1 on API 34 and 1/1 on API 36; no compatibility fallback or skipped device exists |
| P28-E004 | Actual-device encrypted staging, 100,000-row scale and process recovery | `./gradlew :transfer:data:pixel6Api34DebugAndroidTest --no-configuration-cache --max-workers=1 --console=plain`; focused commit-descriptor replay after API-28-safe fixed-length decoding | Independently keyed seven-table staging streams and pages 100,000 rows; database bytes contain no sentinel plaintext; encrypted source handles and commit descriptors plus mappings/duplicate candidates survive reopen | PASS 5/5 on Pixel 6 API 34 in 4m33s; the four SQLCipher staging tests consume 249.263s. The final fixed-length descriptor reopen replay also passes 1/1 in 36s |
| P28-E005 | Primary/shadow atomicity, idempotence, audit and whole-batch undo | `./gradlew :finance:data:pixel6Api34DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.ledger.finance.data.ImportFinancialApplicationPortDeviceTest,app.ledger.finance.data.StructuredImportApplicationPortDeviceTest --no-configuration-cache --max-workers=1 --console=plain` | Small batches share one application/coordinator transaction; huge/structured batches apply to a same-filesystem shadow, validate and atomically exchange; source-row 99,999 failure leaves the primary unchanged; replay is idempotent; all 15 structured kinds and whole-batch undo pass | PASS 3/3 on final Pixel 6 API 34 replay in 1m49s; row 99,999 preserves the exact primary snapshot, replay appends nothing, and financial/structured audits plus whole-batch undo restore the pre-import state with injected deterministic ID ownership |
| P28-E006 | IMP UI, privacy, accessibility, responsive/localized matrix and pixels | `./gradlew :feature:transfer:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --console=plain`; `ImportUiContractDeviceTest` | IMP-001—IMP-010 and all 34 states render at 320/360/480dp, 100/130/200% font, zh-CN/ja-JP/en-US and light/dark; 100,000-row preview is virtualized, sample data is absent from semantics, duplicate decisions remain explicit and pixels match production baselines | PASS 4/4 on final Pixel 6 API 36 replay in 53s; fixed non-sensitive semantic IDs pass source policy and pixel SHA-256 values remain `12f8bfa6a52add008a6950783b219dafb642fa6fb366790532f2e7423ad09d96` and `175eb93bb5588cda9e9eef31bdefc84af52cbce53014942b5aa48d520c7cce53` |
| P28-E007 | Formatting, Detekt, Lint, architecture and application-root regression | `./gradlew p28Check --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain`; `./gradlew :app:pixel6Api36DebugAndroidTest --no-configuration-cache --max-workers=1 --dependency-verification=strict --console=plain` | Frozen dependency directions, coordinator/planner boundary, operation privacy, exact locks, formatting, Detekt, P28-relevant Lint/JVM tests and application navigation/Worker integration pass | PASS: final 922-task aggregate completes in 31s; architecture covers 40 projects, source policy 244 production files/215 IDs, and full API 36 application suite passes 11/11 in 1m15s with the injected ID source and merged `dataSync` service |
| P28-E008 | Frozen-source baseline, complete script regression and repository hygiene | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_spec_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_p01_baseline.py`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*.py'`; `git diff --check`; production placeholder/direct-write/privacy/no-POI scans | Ten frozen files and all exact counts remain unchanged; all phase/mutation tests pass; 37 lockfiles and 1,382 verification components/2,848 SHA-256 entries are complete; no patch defect, excluded-draft input, placeholder, fake persistence or P29+ promotion exists | PASS after final replay: P00/P01/P28 validators pass, all 161 script tests pass, `git diff --check` is clean, production placeholder/POI scans return no match, and the visual-access guard remains fail-closed |

P28 is `VERIFIED`. Evidence covers 100,000-row streaming and encrypted staging, API 28/34/36 parser compatibility, crash/pause/cancel recovery, all 15 structured kinds, row-99,999 atomic failure, replay idempotence, audit/whole-batch undo and every IMP-001—010 state. P29 and later stages remain unpromoted.

## Future mandatory evidence classes

| Class | Minimum acceptable evidence | Earliest phase |
|---|---|---|
| Build/static | Frozen Gradle aggregate task, lint, detekt, formatting, dependency locks/verification, architecture checks | P01/P02 |
| Domain | JUnit 5/Kotest property tests for all applicable accounting and deterministic invariants | P03/P06 |
| Database | Room migration helper plus actual-device SQLCipher/FTS5/R*Tree/WAL/integrity/projection evidence | P07/P08 |
| Security/device | Actual-device Keystore, BiometricPrompt, app lock, vault, clipboard and screenshot behavior | P09/P32 |
| Platform/device | Actual-device SAF, location timeout/permission and foreground/UIDT behavior | P10/P28—P31 |
| UI | Compose UI, screenshot/golden, three languages, width/font/theme matrix, TalkBack manual run and semantic leak audit | P04 and feature phases |
| Performance/fault | Macrobenchmark/profile, target-scale generators, query plans, streaming memory, cancellation, crash recovery and fault injection | P28—P35 |
| Release | Release AAB, Baseline Profile, SBOM, licenses/NOTICE, locks, signing/config checks and full acceptance report | P36 |

No future evidence is marked PASS in P00.
