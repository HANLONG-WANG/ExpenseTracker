# UI 审查修复进度

> 依据：`UI.md`。本文件仅记录修复进度，不改动原审查报告。

## 2026-08-20

- [x] `6.3 / REC-001`：三语顶栏标题统一为顶层页面标题“记账 / Record / 記録”，移除错误且不会随 Tab 更新的“支出”后缀。
- [x] `8.4 LedgerSaveFab`：`enabled = false` 或 `submitting = true` 时保存回调改为不可触发，并补充 disabled 无障碍语义。
- [x] `8.9 MetricCard`：可点击卡片显示明确的右向箭头，卡片本身继续提供按钮点击语义。
- [x] `8.10 StatusBadge`：Candidate 使用轮廓样式，并由组件保证显示本地化“未入账”文案。
- [x] `8.11 CategoryTile`：选中勾移至右上角；删除分类增加本地化“已删除”可见文字，保留灰色图标与删除线名称。
- [x] `8.15–8.21 / 13.2 / 13.6`：Toggle 的 on/off、FormSection 的 expanded/collapsed、表格页码与敏感值倒计时改用中英日资源，不再硬编码英文或直接拼接单位。
- [x] `OnboardingScaffold`：步骤进度和返回箭头进入顶部栏；“稍后设置/继续”改为固定底部操作；submitting 增加明确的本地化保存中反馈。
- [x] `ONB-002`：默认本位币改为 JPY；选择数据源改为运行时完整法定货币目录，并支持按代码或本地化名称搜索。
- [x] `ONB-003`：候选数据源改为完整 IANA 时区集合；显示系统时区、当前时间和所选时区当前时间；生产初始化仅在系统时区确为 Asia/Tokyo 时预选该值。
- [x] `ONB-005`：未记录诊断选择时，Onboarding、应用运行时和设置 UI 的功能统计/崩溃报告均默认关闭。
- [x] `ONB-006`：状态接入真实设备安全能力；页面始终显示能力说明，无安全锁屏时禁用应用锁并解释限制。
- [x] `ONB-007`：增加明确的备份位置卡，显示尚未配置及后续本地目录/Drive 配置路径。
- [x] `ONB-009`：内嵌分类创建增加中央图标/颜色选择器，选择值实际写入首个分类命令。
- [x] `6.3 / REC-001`：记账顶栏在“更多”之前增加始终可达的“模板”操作，空账本也可进入 REC-026。
- [x] `REC-001`：支出和收入分别持有独立 `LazyGridState`；其他交易改为严格六项且每项含图标、标题和用途说明；移除错误的第七项“批量录入”。
- [x] `REC-001 / REC-026`：快捷模板在首页和模板选择页均显示分类图标、名称及可解析时的地区化金额。
- [x] `REC-002`：搜索结果改为复用 `CategoryTile`，二级分类显示父分类副标题。
- [x] `REC-005`：历史编辑会保留当前已归档账户的可见性，新交易仍只列活动账户。
- [x] `REC-006`：无实体卡时增加解释性空状态与管理入口，不再只剩“无”单选行。
- [x] `REC-012`：路由内容改为契约要求的六项 `OtherTransactionGrid`，不再错误打开高级统计页。
- [x] `G-003`：Opening 使用真实 UI presentation；数据库文件与当前 schema marker 不一致时在打开前进入 migrationDetected，数据库不可用进入可重试 failed；opening 继续遵守延迟显示。
- [x] `G-003 生产接线复核`：schema marker 现为 ViewModel 级依赖，并在每次 `openSavedBook` 真正打开账本前决定 `opening/migrationDetected`；移除恢复密码步骤中的错误局部声明，避免生产源码无法解析且状态仅在 ONB-007 偶然更新。
- [x] `G-004`：维护 presentation 由真实维护原因和恢复执行阶段驱动，覆盖 preparing/running/nonCancelable/cancelable/failed/succeeded；可取消阶段接入取消回调，失败的中断恢复接入重试；操作名改为中英日本地化文案。
- [x] `G-005`：按 SCHEMA_INVALID/KEY_UNAVAILABLE/PROJECTION_FAILURE 映射 corrupt/keyUnavailable/projectionFailure；成功验证并发布备份时写入不含敏感数据的可恢复标记，恢复页据此进入 restoreAvailable/noBackup，不再用诊断码猜测备份可用性。
- [x] `G-006`：More 首页将分类、商家、地点、卡片、外观、语言与地区、币种、应用锁、隐私/诊断和关于拆为可直接点击的独立入口；卡片入口使用真实账户参数，无账户时回到账户列表，避免空 `accountId`。
- [x] `G-008 / P33 生产可达性复核`：修正 More 已接收却未渲染 `onHelp` 的死回调；设置分组现在显示本地化帮助入口，并进入离线帮助白名单中的 getting-started 主题。
- [x] `P11 外壳证据复核`：app-shell 门禁现联合读取拆分后的 `AppRootScreen + ReadyRootScaffold`，并新增 NavDisplay、底栏和未保存内容提示的 mutation 保护；DAO/Entity 禁令只匹配真实 import/类型，不再把 `EntityStatus` 误判为数据层泄漏。
- [x] `5.6 / 13.5`：Vico 图表正常模式使用 token 的 Standard 220ms 缓动；减少动画时位置/尺寸直接完成并仅做 80ms 淡入。Theme 同时读取应用设置与系统 animator 开关，MapLibre 聚类在减少动画时改为直接移动镜头。
- [x] `5.7`：分类、Choice/Toggle、Chip 切换及校验错误保留触觉反馈；普通交易、退款和专项交易仅在真实提交成功后由 ViewModel 发出一次性触觉事件，分类/仪表板拖动在开始拖动时反馈。
- [x] `9.1 / 13.2`：普通交易错误摘要点击会滚动并聚焦对应字段；错误首次出现时自动移动到首个错误字段，同时保留 Assertive 公告。
- [x] `9.3`：金额为空且从新建/模板进入时自动聚焦，所有 `SearchField` 打开时自动聚焦；`LedgerTextField` 的 Next/Previous/Done 默认移动焦点或收起键盘；分类搜索和流水等跨目的地列表使用 saveable `LazyListState` 恢复位置。
- [x] `REC-003 日期时间`：编辑器日期时间改用当前应用语言的地区化 medium/short 格式，不再硬编码 `yyyy-MM-dd HH:mm`。
- [x] `REC-003 编辑冲突`：冲突卡增加“查看差异”并通过生产路由进入该交易的修订历史；重新载入/取消继续保留，冲突态不会分发覆盖保存。Compose 交互测试同时断言差异导航和零保存调用。
- [x] `8.23`：`AttachmentField` 为 FAILED 显示“未保存”状态和重试入口；普通/专项交易与附件基础设施把真实失败状态和重试动作接入该行。
- [x] `8.25 / 14.1–14.6`：中央 Vico 包装按稳定 series key 映射冻结 categorical token，轴/网格/选择态使用专属 chart token；线图具有 2/3dp 线、6dp 圆/方点、虚线与缺失值断线，柱图含 4dp 间距、零轴和长标签多系列横条回退，饼图超过六类在生产路径自动回退并解释；统一文字图例、280dp 高度、精确值 explorer/marker、左右键与无障碍动作支持首尾循环，堆积图显示总计，空态和可访问数据表始终保留；`ChartCard` 只组合一次图表。
- [x] `8.26`：`AccessibleDataTable` 模型增加尾端对齐列信息，金额/度量列使用等宽数字并右对齐。
- [x] `8.28 / 13.2`：操作失败面板增加可展开的脱敏错误详情及可选临时文件清理入口；长任务公告内容按 5% 桶和阶段变化稳定，不再随每个 processedText 更新朗读。
- [x] `JRN-001`：搜索/筛选移入顶栏；默认预设与所有有效条件显示为可移除 Chip；Paging 刷新保留旧数据并使用局部状态，追加加载使用紧凑行内进度；交易类型与无障碍描述使用三语固定文案，金额格式化器按列表复用并记忆行模型。
- [x] `JRN-001 日期吸顶复核`：把普通列表内日期卡改为真实 `LazyColumn.stickyHeader`；分组只遍历 Paging 当前已载入快照并在行组合时继续访问原 `LazyPagingItems` 触发预取，不恢复全量 `peek` 扫描或把完整结果物化为 List。
- [x] `JRN-002`：搜索框自动聚焦，框下范围改为由当前真实筛选生成，不再显示固定说明。
- [x] `JRN-003`：筛选覆盖位置/半径并移除重复退款项；日期时间改为地区化日期/时间选择流程，金额改为按所选币种输入主单位；类型/状态/来源/统计性质全部本地化；自然语言摘要固定在顶部，重置/应用固定在底部。
- [x] `JRN-005`：选择操作固定在列表上方，批量编辑操作固定在列表下方，Paging 列表独立滚动。
- [x] `JRN-006`：增加实时变更摘要与固定原子提交栏；日期时间使用选择器，统计性质本地化；validating/committing/failed/succeeded 均提供明确、专属且本地化的状态反馈，失败可直接重试。
- [x] `JRN-007`：位置改为独立区块，增加状态/来源区与最近三次历史预览；补齐编辑、基于当前交易发起退款、复制并预填快捷模板，以及原有回收站操作；创建/修改时间和统计性质均按地区与固定文案显示。
- [x] `JRN-008`：修改历史改为带时间线标记的版本卡，动作、时间、来源和字段变化均本地化；历史版本增加直接“以此版本创建新修订”操作。
- [x] `JRN-009`：变化/未变化标题使用三语资源，未变化区默认折叠，并把字段键转换为面向用户的固定文案。
- [x] `JRN-010`：依赖类型、子交易状态、关系和总体影响说明本地化；数据查询补充父/子交易可读标签，界面不再把 StableId 当作主要说明。
- [x] `JRN-011`：回收站行补充地区化删除时间、预计清理时间、依赖数量、明确选择状态，以及行内恢复和永久清除入口。
- [x] `JRN-012`：不可清除原因和依赖摘要使用三语文案；资格验证与永久清除分别显示明确阶段进度，验证失败可重试。
- [x] `JRN 交互语义补漏`：已保存筛选与依赖空状态不再显示无效“重试”，而是执行真实返回；批量卡片选择不再伪装为可独立启用字段；详情交易行与状态 Chip 明确为只读；复制筛选的生成名称按应用语言本地化。
- [x] `JRN 汇率证据补漏`：详情按地区格式显示明确汇率方向，并逐项显示手动/提供方来源、账本时区报价时间与使用时过期状态；无效 domain rate 不再原样泄漏。
- [x] `ACC-001`：账户顶栏增加全局金额显示/隐藏开关，账户卡、净资产、目标、流水和图表统一响应；金额按当前地区与真实币种格式化，外币 secondary value 使用本位币语义。
- [x] `ACC-003`：现金/银行账户增加可选账号字段；币种改为受支持法币目录选择并提供字段级校验；新建信用账户保存后进入信用设置，新建贷款账户保存后进入贷款合同向导。
- [x] `ACC-004`：期初余额改用统一金额表达式字段和地区化日期选择器，外币账户补充本位币金额校验，不再输入 minor unit 或 ISO 日期文本。
- [x] `ACC-005`：可用余额扣除该账户已保留的目标资金；本位币估值显示独立数值与地区化更新时间；无流水时由对应空状态替换近期流水内容。
- [x] `ACC-006`：增加全部/支出/收入筛选行；交易列表接入 Paging 3、稳定键及刷新/追加状态；金额与运行余额右对齐，Compact 下运行余额移动到金额下方，日期和金额均地区化。
- [x] `ACC-007`：检查点使用统一金额表达式字段和日期选择器，并同时提供查看附近流水、创建余额调整、仅保存检查点三个明确操作及保存中反馈。
- [x] `ACC-009`：实体卡按活跃与归档分区，归档卡继续保留历史入口和显示。
- [x] `ACC-010`：增加独立可点击的保险库设置入口；名称、兼容账户与尾号校验直接驱动字段状态和保存可用性。
- [x] `ACC-011`：活跃卡同时显示创建替换卡与归档操作；替换编辑器单独保存 replacement 关系，不覆盖原卡尾号。
- [x] `BUD-001`：月份标题按当前地区格式化；Hero 独立显示可用总额，今日可用卡独立显示尚未发生周期固定支出；分类行增加图标、货币化进度和一级展开/折叠二级交互。
- [x] `BUD-002`：月度预算编辑改为底部固定保存栏；总额、字段与父子约束共同控制保存可用性，约束卡全部使用本位币格式而非 minor unit。
- [x] `BUD-003`：分类预算增加父级/子级逐字段约束错误，超限或无效时阻止保存，并使用固定保存栏和货币化约束摘要。
- [x] `BUD-004`：补充归档分类结转转移入口；历史调整使用本位币金额、地区化时间和明确的调整版本号。
- [x] `BUD-005`：保存栏固定在底部，并由金额、来源/目标分类和 saving 状态共同控制；invalid/saving 显示页面级反馈；归档结转路径选择归档来源与活动目标。
- [x] `BUD-006`：修订历史改为带标记的时间线，金额与时间地区化；比较操作不再重试加载，而是打开最新两个修订的真实总额与分类限额对比。
- [x] `BUD-007`：预算模板总额按本位币和当前地区格式显示。
- [x] `BUD-008`：模板编辑复用受约束的固定保存栏；路由模板 ID 必须解析到实际载入模板，否则显示明确 not-found 状态。
- [x] `PRJ-002`：开始/结束日期改为地区化日期选择器；关联目标改为可展开的真实单选列表；保存操作固定在底部，由完整校验和 saving 状态驱动。
- [x] `PRJ-003`：项目期间在列表与详情均地区化显示；无交易时在近期交易区域显示专属空状态和项目流水范围入口。
- [x] `PRJ-004`：增加明确的项目筛选范围卡；Paging 行复用 `JournalTransactionRow`，交易类型与日期本地化，并可直接打开对应流水详情。
- [x] `PRJ-005`：现金流页增加独立净现金流指标；图表点标签与可访问数据表日期全部按当前地区格式化。
- [x] `GOL-001`：目标状态徽标使用进行中/已完成/已归档三语固定文案，截止日期按当前地区显示。
- [x] `GOL-002`：账户改为可展开的明确单选器，截止日期改用地区化日期选择器；固定保存栏由完整校验和 saving 状态驱动。
- [x] `GOL-003`：资金动作类型、动作时间、截止日期和趋势日期均本地化；趋势图接入已完成的中央 token、图例、轴线、精确值 explorer 与数据表契约。
- [x] `GOL-004`：资金动作日期改用地区化日期选择器；固定保存栏响应校验与 saving，同时保留“余额不足仅警告、不阻止保存”的契约。
- [x] `10.1 / PRJ-GOL 进度语义`：项目预算与目标资金进度的 TalkBack 文本改用真实币种和当前地区格式化金额，不再把 minor-unit 整数直接插入无障碍说明。
- [x] `CRD-001`：补齐本期剩余、已还、临时额度及到期、额度使用进度、最近账单/还款日、账单预览和固定概览/账单/分期 Tab；金额与日期均按地区显示。
- [x] `CRD-002`：增加到期规则类型与自动还款模式；时区改为受控选择、临时额度到期改为日期选择器；既有固定保存操作现在由完整字段校验控制并显示 saving。
- [x] `CRD-003`：增加全部/待还/已付/逾期账单筛选；列表独立显示应还、已还、剩余和状态，预计/官方金额不再拼接 minor unit。
- [x] `CRD-004`：增加真实还款进度、账单交易与还款分配列表；数据端从不可变事实查询并可跳转流水，封存账单可直接检查交易归属影响。
- [x] `CRD-005`：官方金额以地区化货币展示预计值，增加封存开关与 saving 进度；固定保存操作由官方金额校验控制，保存时遵守用户选择的封存状态。
- [x] `CRD-006`：前期/当期/后期改为包含实际周期、状态、剩余金额及“手动”审计语义的选择卡，选择与确认提交分离并提供 saving 反馈。
- [x] `CRD-007`：从目标还款交易读取真实付款总额，展示按账单分配表和剩余差额；账单行可选择改配目标，保留最早未付与未分配预付款策略及不可变修订语义。
- [x] `CRD-008`：重复资格检查改为读取真实自动还款事实；显示默认付款账户名称，资格不足时禁用正式模式；既有固定保存操作继续保存正式/候选模式。
- [x] `INS-002`：增加独立分期本金输入与可选择舍入规则；购买选择展示全部合格交易及可读账户/金额/日期摘要，首期日使用地区化日期选择器；已有固定保存操作仅在真实计划预览生成后启用并提交创建/更新。
- [x] `INS-003`：增加本金偿还可视进度；关联购买改为可读账户、货币化金额和地区化日期卡，并可直接打开对应流水。
- [x] `INS-004`：期次日期改为地区化显示；金额继续使用货币格式并由已修复的 `AccessibleDataTable` 尾端对齐列。
- [x] `INS-005`：结清日期改用地区化日期选择器；模拟结果增加旧计划未来期次与一次性结清后计划的明确差异，并保留完整旧计划表作比较。
- [x] `INS-006`：数据层读取当前计划之前的真实计划修订；退款影响页分别展示旧版与重算版表格，不再把当前计划重复充当差异。
- [x] `13.6 / INS 舍入文案`：分期舍入规则的兜底展示使用三语资源，不再把 Java `RoundingMode.name` 暴露给用户。
- [x] `LIA-001`：负债首页分别汇总本位币信用负债、贷款剩余本金和未来分期承诺，并从未结信用账单、未完成贷款期次及分期计划计算最近到期；content/empty/overdue 语义同时读取三类负债，分期卡展示真实金额、下一期并可进入详情。
- [x] `LOA-002`：合同设置改为可前后返回的六步向导，依次覆盖基础信息、多分项卡、分项条款、利率阶段、计划预览和检查创建；每个分项独立保存本金/条款/利率，必须生成有效预览后才能通过固定保存操作创建，且新建不再误绑定首个既有合同。
- [x] `LOA-003`：分项编辑显示当前条款/计划版本、还款方式与期数摘要；创建态使用独立持久状态并明确提示，不会在输入后退化为编辑首个分项，固定保存操作由名称和本金校验驱动。
- [x] `LOA-004`：条款编辑补齐固定/浮动利率、支付频率、提前还款政策、缩期/降月供策略、条件性违约金及舍入规则；首期日使用地区化日期选择器，固定保存操作由完整条款校验驱动。
- [x] `LOA-005`：增加可新增和点击编辑的利率阶段编辑器、地区化起止日期、重叠区间专属反馈及重新生成未来计划操作；有效无重叠阶段驱动固定保存操作。
- [x] `LOA-006`：增加计划期数、本金、利息、费用及结束日期摘要；表格金额按真实币种地区化并包含手续费，计算失败显示具体校验代码而非通用无效提示。
- [x] `LOA-007`：详情补齐已还本金、已付利息/费用、还款进度和计划/实际差异；增加概览/计划/交易/利率/模拟 Tab 及真实计划分配形成的近期还款列表，多分项继续独立展示条款摘要。
- [x] `LOA-008`：数据端读取全部历史计划修订；页面可轮换选择分项/修订版本并切换计划与实际分配，日期和所有金额均按地区及币种格式化。
- [x] `LOA-009`：详情改为按目标交易查询不可变贷款事实和实际计划分配，只读显示本金、利息、费用、罚息、期次分配及付款账户的自然语言资产/负债/非消费费用影响，不再读取通用输入草稿。
- [x] `LOA-010`：模拟结果增加调整前后未来总支付、利息、费用、期数和结束日期摘要、节省成本指标及明确标注的新旧完整计划表；整个模拟结果使用中性未写账说明。
- [x] `LOA-011`：应用页显示本次总支付、本金、违约金和新计划期数/结束日期的具体影响；版本冲突提供重新加载并计算操作，同时保留二次高风险确认。
- [x] `SET-001`：活动卡显示可读关联项目，并把“需补充结算”状态标到对应活动卡上，不再只有无法定位对象的全局提示。
- [x] `SET-002`：创建态增加真实结算币种选择，编辑态显示已锁定币种；起止日期改用地区化日期选择器，移除列表内重复保存按钮并由固定保存操作响应完整校验和 saving 状态。
- [x] `SET-004`：参与人行补齐代付、应承担、已结算和净额；详情直接展示活动交易与近期结算预览，并为没有交易/结算的有效活动显示区块级空状态。
- [x] `SET-005`：关联交易时间按地区显示，应承担金额按活动币种格式化；金额列沿用已修复的尾端对齐数据表。
- [x] `SET-006`：增加地区化结算日期选择器并把所选本地日期写入不可变结算记录；移除列表内重复保存按钮，固定保存操作由金额、参与人、日期及条件账户校验驱动且 saving 时不可重复提交。
- [x] `SET-007`：结算记录时间改为地区化日期时间，付款人、收款人、货币化金额与本地交易关联状态继续组成完整时间线卡。
- [x] `SET-008`：增加逐参与人的历史已结算、当前理论头寸和待补差额表，并在同页展示真实建议转账；历史不可改写警告与补充结算入口保留。
- [x] `AUT-002`：快捷模板增加搜索、名称/最近修订排序、全部/使用中/归档筛选和真实归档操作；无匹配结果显示筛选级空状态。
- [x] `AUT-003`：模板身份增加可选图标字段；交易字段扩展为完整活动分类/主次账户/卡片/商户/项目/目标/互请活动/固定地点选择，不再截断候选，同时继续明确排除实际时间、实时位置、附件和实时汇率；移除列表内重复保存按钮并由校验驱动固定保存操作。
- [x] `AUT-004`：用可交互的全部/使用中/暂停/归档 Tab 取代静态状态说明，并实际筛选周期交易列表。
- [x] `AUT-005`：周期设置显示受控固定地点字段并可直接进入模板修改固定地点；规则摘要改用本地化频率与地区化开始日期，移除列表内重复保存按钮并由有效规则驱动固定保存操作。
- [x] `AUT-006`：每月第 N 个星期规则补充单选星期几；起止日期改用地区化日期选择器；“应用规则”只校验并把草稿返回周期设置，不再提前保存整个系列，无效规则时操作禁用。
- [x] `AUT-007`：未来发生项的本地日期、固定发生时间和 occurrence instant 均按当前地区及系列时区格式化，不再显示 ISO/Instant 内部字符串。
- [x] `AUT-008`：选择态增加逐项检查与批量跳过操作；候选校验错误映射为用户可理解的固定文案，不再泄漏内部 code，Candidate 继续使用已修复的轮廓徽标。
- [x] `AUT-009`：确认入口明确进入既有完整交易编辑器，只有完整表单保存成功才接受候选；页面提供确认、跳过和取消三项操作，失败继续保留候选与编辑输入。
- [x] `ANA-001`：期间改为按当前地区格式显示；重新计算时保留当前期间和既有摘要/报表骨架，仅在内容内显示计算进度，不再清空整页。
- [x] `ANA-003`：报表增加本地化 `MetricSummary`；重新查询时保留期间、旧图表和数据表并显示局部进度，比较期间与图表点日期均地区化，系列名称不再泄漏 enum。
- [x] `ANA-004`：指标、维度、粒度和比较改为可直接选择的完整本地化选项；接入真实账户/分类/商户/地点/项目候选并写入 `ReportSpec`，筛选 Chip 的移除与重置有效；应用操作固定在底部且无效状态禁用。
- [x] `ANA-005`：明细改为复用标准 `JournalTransactionRow` 的自动分页列表，移除手动加载更多；分析查询补充真实分类、账户、卡片和商户标签，交易类型、徽标与日期均使用面向用户的本地化语义，并可打开流水详情。
- [x] `ANA-007`：仪表板卡增加可拖动排序把手和直接编辑源报表入口；保存迁移到固定底栏并由名称校验控制可用性，同时保留上移/下移的无障碍替代操作。
- [x] `ANA-008`：手机端改为真实的七步构建流程，逐步覆盖指标、维度、真实筛选、粒度/比较、排序编辑、兼容可视化及预览/保存；所有选项本地化，预览/保存固定在底部并响应校验和预览中状态。
- [x] `ANA-009`：全部可视化使用本地化标题；不兼容项禁用并明确显示建议的兼容图表，饼图降级说明继续保留。
- [x] `ANA-010`：导出格式本地化，新增仅当前结果/当前与比较期两种真实导出范围；继续操作接入既有完整导出流程中的字段选择、系统文件位置、可取消进度和结果页，不再把本页描述为“仅接口”。
- [x] `ANA 展示边界补漏`：复制自定义报表生成的新名称按应用语言本地化，不再由控制器硬编码英文 `copy`。
- [x] `ANA 图表交互补漏`：报表、自定义报表预览和预测图表的数据表操作均驱动真实展开/折叠状态；分析下钻交易行不再声明无效长按动作。
- [x] `ANA-013 / 31 禁止滚动保存`：异常规则编辑器使用校验驱动的固定底部保存操作，列表内不再放置随 finding/规则滚走的保存按钮；每个异常 finding 同时以本地化算法名称作标题，并逐项披露算法版本、阈值、得分、地区化窗口、货币化观察值/基线及相对影响，不再显示内部 `explanationCode`。
- [x] `ANA-014`：预测图表增加当前已观察值到月底预测的趋势和历史同月比较系列；可访问表格同步列出模型，模型窗口/截止日地区化，观察值、每日平均和周期支出均按本位币格式显示。
- [x] `ANA-015 / P25 完整性复核`：全量 projection mismatch 审计现调用 `AnalyticsProjectionEngine.staleTables` 并将异常归入 `ProjectionFamily.ANALYTICS`，避免仅有 generation 正确却放过缺失/空分析表；分析草稿同时明确保持在内存，名称和筛选值不进入 route/SavedState。
- [x] `P25 可访问表格证据复核`：校验器把 `AccessibleTableUiModel/reportTable` 约束到主 `AnalysisScreens.kt`，不再允许 P26/P27 文件中的同名 token 掩盖主报表数据表被删除；对应 mutation 再次能够可靠失败。
- [x] `ANA-010 / P26 导出证据复核`：移除校验器对旧“仅接口”实现的反向要求，改为强制存在导出范围选择、完整流程文案与 `actions.onPrepareExport`；新增断开完整导出动作即失败的 mutation。
- [x] `CAT-001`：分类搜索输入会进入可见 searching 反馈并在本地筛选完成后恢复结果，不再只有 content/empty 外观。
- [x] `CAT-002`：增加使用当前 palette container/foreground token 实时计算的 `ContrastPreview` 与比率警告；名称、对比度和 validationError 共同控制固定保存栏。统计性质变化会显示高风险说明并提供进入逐项选择/确认的批量编辑重算流程入口。
- [x] `CAT-003`：分类树增加纵向拖动把手、跨项重排和阈值触觉反馈，同时保留首尾正确禁用的上移/下移无障碍替代操作。
- [x] `CAT-004`：processing 时三个处理操作全部禁用，防止重分配、归档或墓碑删除重复提交。
- [x] `MER-002`：逗号文本框替换为可逐项添加、去重和移除的 `AliasList`；名称/重复校验驱动底部固定保存栏。
- [x] `PLC-002`：创建态也生成可见草稿图钉；原始 E7 文本框替换为 `MapPinEditor`，可点按/长按地图移动并用无障碍方向按钮微调，显示十进制度数摘要；地图失败保留可访问替代，保存改为校验驱动固定栏。
- [x] `PLC-003`：地图同时展示来源地点和每条位置记录的实际坐标分布；拆分选择项使用地区化捕获时间、十进制坐标、当前交易数及地图点归属反馈，本地 Tab 直接产生 merge/split 生产交互且无效提交被禁用。
- [x] `VLT-001`：保险库列表现在默认显示独立锁定状态和真实设备认证操作，通过认证后才显示卡片并明确会话解锁范围；进入后台会重新锁定，空状态操作改为进入账户与卡片而非错误打开系统安全设置。
- [x] `VLT-003`：所有敏感输入保持视觉掩码，并以非空字段标签作为可访问语义；自定义秘密改为可逐项添加、命名、输入和移除的字段列表，未完成/超长内容阻止保存；加密保存迁移到固定底栏且 saving 时不可重复提交。
- [x] `SETG-002`：外观页的实时预览现在渲染真实金额、卡片、进度图形与按钮组件；主题、动态颜色及默认金额隐藏设置会直接作用于同一组件树。
- [x] `SETG-004`：币种按“可见/隐藏”两个契约区块展示；可见顺序仍使用完整持久列表，首项上移与末项下移会正确禁用，搜索不会破坏边界判断。
- [x] `SETG-006`：状态从 Android Keystore 的实际设备安全能力派生，明确区分“强生物识别或设备凭据”“仅设备凭据”和不可用，不再显示固定泛化摘要。
- [x] `SETG-009`：核验现有生产实现已显示关闭即删除对应队列和安装标识、重新开启生成新标识的完整说明；`diagnosticsChoiceRecorded=false` 时 Onboarding、运行时及设置均已默认关闭，无需重复改写。
- [x] `SETG-010`：功能事件时间按账本时区和当前地区显示；空队列改为无破坏操作的只读空状态，只有存在事件时才显示删除队列。
- [x] `SETG-011`：崩溃报告时间按账本时区和当前地区显示；空队列不再提供删除空队列操作。
- [x] `CLR-001`：页面逐项列出本机账本/设置、附件对象、设备绑定密钥与本机管理型备份；确认后显示真实设备认证门的待处理/完成状态并防止重复提交，失败状态映射为用户可理解的安全结果，不再泄漏内部 errorCode。
- [x] `IMP-001 / IMP-003`：导入源页增加最近导入历史入口；字段映射增加必填状态汇总，缺失必填字段会真实禁用继续操作。
- [x] `IMP-004 / IMP-005`：实体映射增加创建/映射冲突汇总及逐来源值的现有实体轮换映射；币种页增加源币种映射与“文件历史汇率/手工汇率”策略，提交准备会逐行执行所选策略。
- [x] `IMP-006 / IMP-007`：校验页增加错误/警告分组、行号/字段定位和包含错误的预览语义；预览逐类型显示将创建的账户/分类/商户数量，并使用明确“提交导入”操作。
- [x] `IMP-008 / IMP-009 / IMP-010`：导入执行页增加完整阶段时间线、取消清理、查看错误与临时文件清理入口；结果使用实际交易/账户/分类/商户计数并区分 partialNotAllowed/rolledBack；历史卡增加资格状态、查看结果和真实批次回滚操作。
- [x] `10.3 / 13.6 / IMP 展示边界`：导入映射字段、实体类型、重复匹配原因、校验字段/错误说明与预览状态全部改为三语固定文案；导入历史及 typed cell 预览按应用日期格式、账本时区和地区数字格式生成，不再显示 enum 名、代码改写英文、`READY`、ISO 日期或 `Instant.toString()`，canonical staging 值保持不变。
- [x] `EXP-003`：选择位置与开始执行拆为两个真实动作；目标 URI 和覆盖确认只准备导出，固定“开始导出”会重新校验目标后才启动任务。
- [x] `BKP-001`：备份首页显示最近快照、最近失败的面向用户文案及下次自动备份策略，不再拼接内部 failureCode。
- [x] `BKP-004 / BKP-005`：设置保存迁移到固定操作栏，保险库前置条件以显著 Banner 和设置入口呈现；历史增加仓库筛选，快照卡显示位置与保险库包含状态。
- [x] `BKP-006 / BKP-007`：详情增加真实删除操作（Drive 删除进入认证门）；手工备份提交前显示数据库/设置/附件空间估算，成功后可直接打开新快照详情。
- [x] `RST-001`：本机仓库与 Drive 不再自动挑选最新项；授权/载入后显示来自真实备份目录的快照选择卡，选择具体快照才进入密码页。
- [x] `RST-003`：检查页使用真实物化结果分别显示本位币、对象数、逻辑字节、附件数和保险库内容，并逐项呈现 schema、迁移、SQLCipher、AEAD/哈希、外键、日记账、投影、交易子类型、附件、账本身份和本位币检查结果。
- [x] `RST-005`：冲突类型改为三语固定文案；共同祖先/本机/备份使用地区化版本代数与缺失状态比较，在当前 merge model 仅提供不可拆分版本元数据时保持整项选择且不伪装字段级合并；增加真实“应用到同类冲突”批量解析逻辑，并移除内容哈希、实体枚举和 StableId 的可见泄漏。
- [x] `RST-006`：恢复执行页增加可滚动完整阶段时间线，密码认证、对象校验、迁移、完整性检查、投影重建、重建验证、交换准备、原子交换、实时验证和回滚均独立显示历史/当前/待处理状态。
- [x] `CLR-002`：云端快照卡显示日期、Drive 位置、验证与保险库摘要；认证完成且至少选择一个快照后才出现高风险确认，删除中显示不可取消的远端删除验证进度。
- [x] `10.3 / BKP-RST 快照时间`：备份历史、备份详情、恢复来源和云端清理中的快照创建时间统一由应用语言、用户日期格式与账本时区生成，不再由控制器暴露设备时区 ISO 日期时间。
- [x] `13.6 / BKP-RST-ATT 展示边界`：备份逻辑内容、对象数、物理增量和预计空间改为 UI 端地区化数字/文件大小与三语资源；恢复结果不再由控制器注入英文摘要，冲突卡不再展示内部实体枚举/ID；附件元数据时间和文件大小统一遵守应用语言与账本时区。
- [x] `ATT-001 / ATT-002 / ATT-003`：新增生产 ATT 路由分发和仅在附件目的地存活的加密读取会话；普通/专项交易附件行可打开安全预览，重命名真实更新显示名称，外部打开在风险确认后签发一次性只读 provider grant；应用锁定会关闭会话并清除图像缓存。
- [x] `SYS-001`：位置字段在系统权限缺失时先进入真实权限说明 Dialog；继续操作发起细粒度/粗粒度系统授权，拒绝与永久拒绝状态由平台结果驱动，后者打开应用系统设置，授权成功才进入位置选择页。
- [x] `WGT-002`：内容选择与隐私页预览改为复用真实 widget snapshot 数据；账户、信用卡和目标选择会显示对应名称、余额/到期/进度布局，其他类型显示各自书本指标与比较结构，金额继续遵守逐组件隐藏授权。
- [x] `8.22 FilterBuilder`：公共组件使用可独立滚动的筛选内容和固定底部重置/应用栏；消费地图的嵌套筛选同样接入固定操作。
- [x] `15.2 组件 API`：公共 `LedgerCard` 改为封闭 `LedgerCardVariant`，公共 `LedgerIconView` 改为封闭 `LedgerIconTone`；任意 `Color` 参数仅保留为 design-system 模块内部重载。
- [x] `15.6 / 16.5 流水列表`：日期分组边界由数据适配器跨 Paging cursor 计算并随行模型传递，组合层不再全量 `peek` 快照；列表使用 `LazyListState` 与稳定交易 key，并在列表级复用货币 formatter、按未变行模型 `remember` 格式化结果。
- [x] `10.1–10.4 格式化`：流水 formatter 失败不再回退显示 minor integer；批量编辑/粘贴使用主单位金额表达式并以真实币种格式显示，日期改用地区化 picker；FX 摘要统一方向、精度、来源、报价/获取时间及手工证据；中央 `LocaleNumberFormatter` 覆盖百分比、利率、计数、期次和权重，地图预览等残留枚举/日期也已本地化。
- [x] `10.8 / 15.5 错误文案`：增加统一封闭 `UiText.Resource`；公共错误状态固定呈现“发生了什么、数据是否改变、下一步”三段，并继续只显示脱敏稳定码；专项交易错误已直接使用资源型文案边界。
- [x] `13.2–13.4 / 13.7 无障碍与隐私语义`：校验摘要可聚焦且错误出现会移动到首错；长任务按 5% 桶与阶段公告；Dialog、BottomSheet、日期/时间模态限制焦点并恢复触发点；图表提供非颜色点形/虚线、标题后 explorer 与数据表顺序；业务金额、名称、地图摘要和操作详情改用 `SemanticsProperties.Text`，`contentDescription` 仅保留控件/图标用途或隐私化状态。
- [x] `10.4 / 13.6 长任务进度语义`：5% 分桶后的 TalkBack 进度使用当前地区的百分比格式器，不再拼接 ASCII `%`。
- [x] `10.3 账本时区补漏`：Theme 提供由应用设置驱动、非法值安全回退 UTC 的账本时区；账户估值、外汇证据、贷款操作、流水筛选/历史/回收站、预算/项目/互请及引用管理审计时间不再读取设备默认时区；每日自动备份边界也改为账本时区计算。
- [x] `15.3 Screen API`：11 个 ready feature 各自在 `*Routes.kt` 中通过 `EntryProviderScope<LedgerDestinationKey>` 注册 Navigation 3 entry；运行时 destination key 按 feature 区分，app shell 不再用单一匿名 entry 承担全部 feature 路由。
- [x] `15.4 UiState / UiAction / effect`：每个 Navigation 3 entry 生成不可变 `LedgerScreenUiState`，入口只接收封闭 `LedgerScreenUiAction` dispatcher；返回/跨页导航与一次性 `LedgerScreenEffect` 分离，既有 callback adapter 限制在入口以下。
- [x] `15.12 / 16.1 Preview 与组件矩阵`：共享七档 annotation 继续覆盖浅/深、320–600dp、100–200% 与三语；补齐 Batch、Reference/Choice/Toggle/Icon、Dialog、BottomSheet、日期/时间 picker 场景，并扩大自动触控目标、标题、紧凑 200% 图表/批量边界和精确图表 explorer 扫描。
- [x] `16.3 关键 Compose 流程`：15 条主操作/错误路径均增加真实用户动作断言。分类网格、完整分类重选、金额错误定位/舍入和位置超时保存通过真实 `MainActivity`、Navigation 3、TalkBack、SQLCipher 路径完成；互请不平衡、冲突、退款跨月、信用超额、批量错误、回收站、预算层级、候选指标、purge tombstone 与保险库分别点击生产 Destination/固定操作并断言零错误写入或唯一合法动作；小组件使用生产 `ledger://widget/quick/...` Intent 打开预填空表单，明确断言不会直接提交，随后才由显式保存写入。P34 门禁逐项要求这些交互证据并包含删除任一关键流程即失败的 mutation test。
- [x] `16.4 无障碍测试`：核心可操作控件统一扫描 48dp 与非空用途语义，标题节点不得为空，浅/深主题正文、语义色和 16 组分类色对执行 WCAG 4.5:1 对比检查；200%/灰度/隐藏值/图表摘要与数据表继续覆盖。API 36 TalkBack 流程已改为真实 `MainActivity`、真实 onboarding/SQLCipher、生产分类网格→金额编辑→保存→返回网格，不再渲染测试专用假页面。
- [x] `REC-009`：位置页接入共享 MapLibre 地图、保存位置点和当前/手工图钉；真实三秒前台定位结果驱动 locating、located/精度、permissionDenied、timeout 与 mapUnavailable 六种生产状态，可在地图移动图钉并把最终 E7 坐标随交易写入；地图失败保留带地点与坐标的无障碍列表，且可明确使用所选位置或无位置返回。
- [x] `REC-010`：普通与专项交易附件从加密对象会话读取真实显示名、大小及 MIME；共享附件行按图片/文档/其他类型显示语义图标，导入失败生成可重试失败行，ready 行可进入生产安全预览及外部打开，临时失败行不会误触无效附件索引。
- [x] `REC-011`：不平衡提示逐项显示付款合计、承担合计与交易总额，所有本人/他人/参与人份额按活动币种和地区格式化；互请不平衡时生产固定保存操作真实禁用，平衡后才允许提交。
- [x] `REC-013 / REC-020 / REC-021 / REC-022`：转账、余额调整和外币兑换改用地区化日期时间选择流程并保留账本时区；期初余额使用地区化日期字段与日期选择器；专项转账附件同时接入真实元数据与安全打开动作，不再点击日期自动加一天或空操作附件。
- [x] `REC-014`：还款表单按要求先显示带字段标签的付款银行账户选择，再显示只读信用账户；还款日期改为地区化日期字段和日期选择器，字段缺失反馈接入现有校验状态。
- [x] `REC-015`：退款现金日期可通过真实日期选择器修改，现金日、应计日和预算月份全部按当前地区格式化，不再回传原值或显示 ISO 文本。
- [x] `REC-018 / REC-019`：贷款放款和还款增加可交互的兼容收款/付款账户选择与地区化发生日期时间；还款另增加未完成计划期次选择，最终写入使用明确选择的账户、期次和 instant/zone，而非隐式取首项或运行时当前时间。
- [x] `REC-025`：批量校验顶部用 `ValidationSummary` 按稳定错误类型分组并显示数量，点击分组跳到该类型首个问题；逐行细节卡与错误阻止提交行为继续保留。
- [x] `REC-027`：分期编辑器增加可搜索、可明确选择的信用账户区，购买候选按所选账户显示且不再截断前三项；首账单日改用地区化日期字段和日期选择器。
- [x] `批量录入格式补漏`：REC-024 不再暴露账户/本位币 minor 或 ISO 文本；同币种权威值随金额表达式自动同步，异币种才显示对应主单位字段，日期/时间通过地区化两步选择器编辑。
- [x] `REC-024 完整编辑器复核`：普通交易与复杂批量行现共同使用 `TransactionEditorScaffold`；分类、账户、实体卡、商户和项目改为可搜索、可明确选择的完整引用列表，账户切换会清理不兼容卡并按新币种重新求值，不再由 ViewModel 循环猜选下一个引用。
- [x] `P34 闭环证据复核`：全局 UI 门禁新增生产回归保护：JRN-001 真实 Paging 吸顶、G-003 打开前迁移态、G-008 More 帮助入口、REC-024 共享完整编辑器与可搜索引用、ANA-015 分析表 stale audit、ANA-010 完整导出动作，以及流水/项目/目标/地图的固定类型文案；对应 mutation 合同与 215 屏/646 状态/90 需求/17 个三语模块校验通过。
- [x] `P25 / P26 / P33 证据版本复核`：阶段校验器跟随当前 45 个分析状态矩阵和加密 schema v1→v4 迁移测试，移除已被后续阶段扩展淘汰的 20/36 状态数及 v1→v2/v3 方法名字面要求；没有用旧数量或伪测试换取通过。
- [x] `11.1 / REC-001`：本节四组差异分别由 6.3 顶栏/模板入口、双 `LazyGridState`、严格六项图文入口，以及首页/选择页共用图标与地区化模板金额闭合。
- [x] `10.2 / 10.5 展示边界`：汇率统一显示方向、精度、来源、报价/获取时间与手工证据；流水筛选/行、项目交易、目标状态/动作、分析明细/地图和导入预览均经三语固定映射，不向 UI 直接输出交易或业务枚举名。
- [x] `13.3 焦点顺序`：公共 Dialog、BottomSheet 与日期/时间模态使用焦点组/恢复边界；图表按标题/摘要→逐点 explorer→数据表排序，并提供可操作的前后点及首尾循环语义。
- [x] `14.2 / 14.3 / 14.4 / 15.9 / 15.10`：线/柱/饼图的线宽、点形、虚线、断线、间距、零轴、横条回退、总计、六类降级、图例、marker 与数据表均由中央 Vico 包装落实；MapLibre 包装读取 reduce-motion 并在减少动画时直接移动镜头。
- [x] `20.1 页面索引`：ATT-001–003 由 `AttachmentRootDestination` 分发且从真实附件行可达，SYS-001 由定位权限前置导航和 `ReadyRootScaffold` 分发；G-003/004/005 的迁移、维护、失败和恢复状态均由 ViewModel/会话/备份事实派生，不再只是 fixture 登记。
- [x] `30 / UI-ADR-005 / UI-ADR-010`：真正禁用的保存控件阻断回调；生产图表调用兼容性解析并对超六类饼图回退且解释，两个曾违反的派生决策均与实现一致。
- [x] `31 禁止实现清单`：原报告点名的项目/目标、分析、贷款、预算、自动化、互请和备份设置保存均迁移到页面外固定操作；中央图表包装也已消除默认第二套视觉。仅“应用规则/合并”等明确的页内结果动作保留在内容流中。
- [x] `20.3 需求追踪`：实现侧 90 条需求和 215 个页面的 VERIFIED evidence 现与生产接线复核一致；P34 不再只检查行存在，还对本报告新发现的吸顶、迁移、帮助、批量编辑、分析投影、导出和类型文案边界执行可变异失败的源码门禁。
- [x] `29 / 33 完成定义与最终冻结结论`：224 条差异记录在 206 个章节全部有显式进度交叉引用，页面字段/动作/状态、固定保存、格式化、无障碍、图表、生产可达性和关键流程证据均闭合；全应用重新满足冻结契约，后续回归由 P04、各阶段合同和强化后的 P34 共同阻断。

## 验证记录

- `UI.md` SHA-256 保持 `bcdbc9573bff31add46cad6a298413898db5e04b3cd00d9ffeae5a2901924e13`，且 `git diff -- docs/testing/ManualTestFindings/UI.md` 为空；P34 会拒绝报告内容、224 条差异数、206 个含差异章节或任一进度交叉引用发生漂移。
- `validate_p04_ui.py` 通过：434 个 token scalar、215 条 route、646 个 state、23 条阶段需求；完整 `validate_p34_ui_closure.py` 通过：215 个 screen、646 个 state、90 条需求、17 个三语模块。
- P10、P11、P13–P16、P24–P33 的生产源码断言通过；P12、P17–P23 的页面/资源断言通过，整阶段脚本只保留与本次 UI 无关的既有加密适配器/领域字面规则。
- P25/P26/P33 自动 evidence 已跟随当前 45 个分析状态矩阵和 schema v1→v4 迁移测试；不再要求旧 20/36 数量或 v1→v2/v3 方法名。
- 全量 `scripts/tests/test_p*_contracts.py` 共 242 条合同/mutation 测试通过；其中 P34 的 25 条 mutation 覆盖报告不可修改、逐节映射和本轮新增的生产接线回归。
- 69 个 Android resource XML 通过解析、重复资源与三语 locale/格式参数闭合；app、core/designsystem、core/files、core/geo、12 个 feature 模块及 widget 共 17 个资源目录均通过 Android SDK 36 `aapt2 compile`。
- `git diff --check` 通过；旧错误标题、硬编码状态、重复图表组合、可见空回调、前三项候选截断和旧“仅接口”导出实现指纹均完成反向扫描。
- 按用户约束未使用 `gradlew`、`./gradlew`、`agent_device` 或 `agent-device`；因此本次不宣称新增的 Gradle/Kotlin 编译或设备执行结果，闭环证据来自上述静态治理、合同/mutation、资源编译及已有生产 Compose 交互/像素基线源码。

## 完成复核

- [x] `UI.md` 的 224 个差异记录点（206 个含差异章节）已逐条复核生产实现与可执行证据；复核中发现并修正的 JRN-001 假吸顶、G-003 marker 生命周期、G-008 死帮助入口、REC-024 简化行编辑器及分析完整性/导出门禁缺口均已纳入可变异失败的回归保护。
- [x] 按用户约束，全程未使用 `gradlew`、`./gradlew`、`agent_device` 或 `agent-device`；验证继续采用静态治理、mutation/合同测试、XML/locale 闭合与 `aapt2` 资源编译。
