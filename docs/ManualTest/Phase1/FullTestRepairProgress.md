# FullTest 修复进度

本文件在阶段 3 开始后逐项追加；阶段 1 与阶段 2 只记录问题，不提前修复。

## 修复记录

- `REG-001`：普通转账与货币兑换的默认账户和选择器现仅允许现金/银行资产账户；信用与贷款账户不再被自动选中，并增加字段级说明，引导信用账户使用专用还款流程。状态：已修复，自动化与设备回归通过。
- `REG-002`：共享静态文本、只读交易行与指标卡改为显式无障碍语义，详情页的标题、业务事实和遵守隐私状态的金额描述不再依赖隐式文本合并。状态：已修复，账户/交易/目标详情的原始可访问性树回归通过。
- `REG-003`：移除普通按钮对表单脏状态的无条件上报；只有实际字段值、选择项或开关发生变化才标记未保存，校验按钮、下一步按钮和选择器取消不再制造虚假修改。状态：已修复，项目校验、预算调整、贷款日期取消与备份目录取消的设备回归通过。
- `REG-004`：预算上一月/下一月改为在预算根页面原位加载月份，不再向预算栈压入第二个 `BUD-001`；历史月份保持全局底部导航。状态：已修复，自动化与设备回归通过。
- `REG-005`：预算调整金额错误改为仅在用户编辑金额或提交校验后显示；全新调整首次打开保持中性初始态。状态：已修复，自动化与设备回归通过。
- `REG-006`：屏幕隐私、回收站设置、隐私与诊断改用稳定页面标题；导入历史根据历史状态显示“导入历史”，不再复用操作名或导入根标题。状态：已修复，四个页面标题设备回归通过。
- `REG-007`：操作中心为真正可恢复的导入/导出/备份任务接入可执行重试；不支持的导入来源不再显示“可重试”，改为明确失败说明与“重新选择来源”入口，新的不支持来源也直接记录为终止失败。状态：已修复，“重新选择来源”真实操作设备回归通过。
- `REG-008`：“更多 > 分类”改为进入基础数据管理中心，再由支出/收入入口携带必需方向参数打开分类列表，不再以缺失参数直达 `CAT-001`。状态：已修复，设备回归确认进入基础数据管理且应用保持前台。
- `REG-009`：单选、复选与开关继续暴露标准角色/选中状态，并把本地化的“已选/未选、开启/关闭”写入可访问名称，禁用状态也显式暴露。状态：已修复，账户选择、回收站单选与隐私开关的设备回归通过。

- `SRC-019`：移除 Material3 顶栏会裁掉状态栏内边距/标题的固定高度，并允许标题最多两行；共享子页面长标题不再被强制压成单行。状态：已修复，待阶段 4 设备回归。
- `RUN-001`：同一顶栏根因已修复；顶层与子页面标题现在由 Material3 自适应高度完整呈现。状态：已修复，待阶段 4 设备回归。
- `RUN-002`：专项交易的转出/转入账户改为可搜索候选对话框，显示账户名、币种、当前选中和确认动作；不再点击即轮换。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-004`：独立退款没有费用分类时保留明确的“创建费用分类”入口，并把分类必填错误显示在该区块，不再误导为金额错误。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-007`：只有一种活动账户币种时隐藏不可完成的外币兑换入口，并显示原因和“管理账户”动作。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-006`：交易详情只渲染确有数据的区块，移除硬编码破折号空占位。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-007`：金额格式化失败时显示本地化的“金额不可用”，不再显示 raw minor-unit 整数。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-010`：流水行父级无障碍描述现与全局金额可见性联动，隐藏时仅暴露“金额已隐藏”；详情金额表达式和退款关系金额也进入同一隐私边界。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-012`：回收站使用专属空态、保留/账务影响说明和“返回流水”动作，去掉无效重试。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-013`：流水 badge、预算标记和退款关系串在 UI 边界转换为三语业务文案与格式化金额，不再呈现内部键/枚举/minor-unit 串。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-014`：未处理依赖时删除入口明确引导“处理关联记录”，领域拒绝时保留可见失败说明。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-015`：版本对比的每个变更字段现在于同一可见卡片中并列显示较早与较新值，不再依赖不易发现的横向滚动。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-016`：退款详情不再被历史/依赖辅助查询失败整页阻断，遗留关系投影异常安全降级；通用错误页不再直接显示内部错误码。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-036`：通用文本的隐私检测改为校验真实 ISO 币种代码，`BSD 2-Clause` 等许可证文本不再被误判为金额。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-001`：实际还款草稿改为持久化用户选中的期次号，并在数据端解析为当前贷款计划项 ID；不再丢弃计划分配。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-004`：账单时区选择器改为全量 IANA 时区、可搜索对话框，并提供无结果空态。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-005`：账户、周期交易、互请活动、贷款与分期的日期选择器空值均改用账本时区的“今天”。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-003`：无债务时不再提供不可提交的提前还款；金额超限使用字段专属说明，账单规则也改为显式单选。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-005`：没有贷款合同时显示解释与“新增贷款”动作，不再隐藏必填入口。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-006`：贷款六步向导为每个中间步骤提供继续入口、无效步骤提示，并把固定枚举改为显式单选。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-027`：信用账户列表的新建入口改为新建信用账户，不再跳入贷款向导。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-028`：账单时区可搜索全部系统 IANA 时区，搜索无结果时有明确空态。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-029`：信用账户与贷款入口使用各自过滤后的列表和标题，二者不再混为同一画面。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-008`：共享交易编辑列表增加固定操作层等高的底部内容留白，最后的项目与后续字段可完整滚入视口。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-009`：商户别名行与删除按钮现在同时暴露别名文本及“移除别名 + 名称”的目标语义。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-017`：账户流水卡补充合并后的类型、日期、金额与运行余额语义，隐私关闭时使用掩码；账户空流水也放入独立的近期流水区块。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-018`：保存余额检查点后稳定返回账户详情并显示全局保存反馈，不再继续压入可能导致任务退出的结果页。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-019`：空账户流水改为有独立标题与卡片边界的空态，不再与实体卡/目标区块共享无边界空间。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-020`：实体卡保存成功后清除暂存账户/替换状态并退出编辑页，表单不再保持脏状态而允许重复新增。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-030`：互请活动列表底部留白提高到固定保存层、系统导航与额外间距之和，最后的参与人输入和“添加参与人”可完整滚入并点击。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-031`：周期规则补齐频率专属字段校验与字段内错误；预览现在直接计算未保存草稿未来 10 次，草稿跨规则/预览页面保留；保存成功会退出编辑态并刷新列表。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-011`：通用筛选 chip 现在暴露可区分的复选角色、选中状态和状态描述；模板/筛选候选不再只是一组无状态文本。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-021`：预算卡在金额隐藏时保留预算名称、周期和资源标签，仅将结构化金额值替换为隐私掩码。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-022`：设计系统文本字段、单选行、复选行和开关行统一补充由可见标签生成的可访问名称；自定义分析的指标、维度、筛选和参数控件不再以无名称节点出现。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-023`：自定义报表编辑器为固定底部操作区与系统导航预留底部内容空间，地点等末尾字段可完整滚入、查看和操作。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-024`：可视化选择会提交选择、清理子页编辑范围并关闭选择页，不再把用户困在工作表/子页面。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-025`：项目详情的概览/交易切换改为替换当前视图状态，不再向导航栈反复压入页面；系统返回现在离开详情而不是倒放标签切换历史。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-026`：目标历史由不可读的宽表改为逐条移动卡片，在窄屏中完整显示日期、类型、金额和说明。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-037`：消费地图的数据查询失败不再整页落入无上下文的通用错误；没有可定位交易时显示明确、可返回的无地点数据空态。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-038`：固定报表、消费地图及其加载/空态被标记为只读路由，未产生编辑时返回不会再弹出放弃更改确认。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-039`：“应用筛选”先提交当前合法表单，再结束编辑范围并返回报表，不再被自己的未保存更改确认拦截。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-041`：重建分析投影成功后显示明确的本地化成功横幅；失败仍保留可重试错误态。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-042`：固定报表目录与报表编辑列表补齐底部安全留白，末尾报表卡/操作不再形成不可点击死区。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-043`：报表上一期/下一期控件使用等宽可点击区域并保留完整可访问名称，较长的“下一期”不再被裁剪成单字。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-032`：字段映射按钮聚合源列、样例和当前目标；实体映射按钮聚合实体类型、缺失数量、当前决策与下一动作；来源值映射也读出当前目标。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-033`：应用私有备份仓库在 Android 将不支持原子移动报告为通用 I/O 异常时也安全回退到同目录替换；失败后切换便携备份再重试会重新打开系统目录选择器；操作中心主错误横幅改为用户文案，内部码仅在主动展开诊断时显示。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-022`：后台任务错误的默认呈现不再直接暴露内部错误码；通用失败说明与主动展开的脱敏诊断详情分层显示。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-040`：导出完成页常驻显示保存目录；“打开”使用有可见反馈的系统处理器选择界面，“查看位置”优先打开目录，无法直接查看时回退到预定位的系统目录浏览器，并在启动失败时显示提示。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-002`：P15 流水详情 mutation 门禁改为针对当前生产的编辑与退款回调分别变异，并同步更新为当前资源和附件控制器语义；257 项合同 mutation 测试全绿。状态：已修复，自动校验通过，待阶段 4 设备回归。
- `SRC-003`：退款、专项交易、预算调整、批量复杂关系以及导入字段/实体映射均改为明确候选选择；退款原交易同时展示可退金额和上下文，不再靠点击次数猜测目标。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-008`：新增统一完整候选选择器，2–8 项显示完整单选，超过 8 项自动进入可搜索模态；账户币种、设置/信用时区、引用管理、项目/目标、互请、贷款/分期、周期模板/地点、信用账单、流水批量编辑等已迁移，生产 feature 不再用固定高度底部面板承载增长列表。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-009`：贷款/分期/信用规则、流水批量字段和依赖策略、自动化排序/规则、分析维度/粒度/对比/排序/异常及消费地图全部改为先展示完整枚举、再选择具体值；字段点击不再直接跳到下一项。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-010`：日期格式提升为全局主题 presentation context，业务日期、日期时间、历史/筛选/图表文本与小组件统一消费中央 formatter；设置切换不会被页面 `remember` 缓存旧格式。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-011`：Glance 本体与配置预览均读取保存的日期格式，百分比按 Locale 格式化，金额格式异常统一安全降级为不可用而非崩溃或显示最小单位整数。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-012`：每周起始日进入全局主题日历上下文；周一/周日覆盖通过 Unicode `fw` 日历 Locale 应用于共享日期和日期时间选择器，设置预览同步显示周标题，底层账务日期不变。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-013`：金额隐私边界扩展到设计系统文本、按钮、chip、Banner、进度、表格与图表；识别到金融金额时视觉和语义同时掩码，图表数据表也不会泄露真实 series；ISO 币种识别避免误伤普通文本。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-014`：账户币种选择器按用户可见币种顺序优先展示，并强制保留本位币、当前账户币种；隐藏币种仍可通过搜索完整目录取得。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-015`：移入回收站按当前 7/30/90/自定义/从不策略写入 `purgeAfter`；新增启动补偿与每日 WorkManager 自动清理，经领域资格/依赖/备份占用评估后才清理，并说明策略只影响新移入项目。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-016`：`REC-006` 与 `ANA-012` 加入统一受控模态集合，分别按 sheet/bottomSheet 契约呈现，关闭恢复原页面上下文。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-017`：P31 校验跟随 `RoomLedgerIntegrityAudit` 的真实审计/投影调用链，P33 要求所有加密前代迁移到当前 v5 证据，各阶段统一接受累计 P36 状态；P31/P33/P34/P36 校验均通过。状态：已修复，自动校验通过。
- `SRC-018`：普通交易首错定位、校验摘要定位和位置地图定位读取全局 Reduce Motion；启用时使用瞬时 `scrollToItem`，正常模式才平滑滚动。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-020`：普通交易、预算、自定义报表最后一步、周期规则、备份设置和保险库保存/应用动作在字段无效时保持可触发；点击后展示字段错误、摘要或无变化反馈，仅提交/权限等绝对前置状态禁用。状态：已修复，编译通过，待阶段 4 设备回归。
- `SRC-021`：Glance 根容器改为消费中央 `LedgerGlanceTokenSubset.contentPaddingDp`，该值由冻结 spacing token 派生，不再本地硬编码 12dp。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-034`：地点编辑器不再默认 (0,0)，新建时以东京为可理解初始视野；地图失败会显示原因、重试和可直接编辑的纬经度，地图点击/方向调整均写回同一图钉状态。状态：已修复，编译通过，待阶段 4 设备回归。
- `RUN-035`：“语言与地区”补齐周一/周日/跟随地区及即时周预览；时区候选只展示规范 IANA ID，并用对应区域名称/偏移生成可区分本地化说明。状态：已修复，编译通过，待阶段 4 设备回归。
